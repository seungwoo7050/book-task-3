# F-realtime-lab Docs

- why HTTP and WebSocket routes are separated
- presence TTL semantics
- fan-out to multiple active connections per user
- Redis as the next scaling step beyond the in-memory test implementation
