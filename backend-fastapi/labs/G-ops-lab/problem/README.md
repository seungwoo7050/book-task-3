# Problem

Build a minimal backend that demonstrates operational readiness rather than product features.

The lab should show:

- live and ready checks
- structured logs
- a metrics endpoint
- container-friendly startup
- CI-facing commands
- AWS deployment shape in documentation
