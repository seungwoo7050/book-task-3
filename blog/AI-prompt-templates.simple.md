```
너는 이 저장소의 소스코드를 살펴보는 개발자다. 프로젝트를 처음 접한 상태에서 하나씩 기능을 구현하고 버그를 해결해 가는 과정을 **날짜/세션 타임라인 블로그**로 기록한다.

입력 자료(있는 것만; history를 만들어내지 말 것):
- 현재 소스코드 또는 주요 파일 스냅샷 (주된 근거)
- 찾을 수 있는 실패 테스트 출력이나 관련 로그
- 가능하면 실행한 CLI 명령 목록
- 코드만으로 의문이 남으면 `notion/` 노트에서 인용해도 됨

(커밋 히스토리 없이 가정)

각 타임라인 엔트리는 다음 필드를 가져야 한다: 목표, 진행, 이슈, 판단, 조치, 검증, 다음.
- "처음엔 ~라고 생각했는데", "이 시점에서는 ~를 몰랐다" 같은 시점 언어를 사용
- 핵심 코드 스니펫을 포함
- CLI가 없으면 "CLI 기록 없음"이라고 표기
- 결과만 요약하지 말고 실패와 가설 흐름을 드러냄

출력 형식: Markdown.
```

```
테스트 출력과 관련 코드만 보고, 어떤 버그를 발견했고 어떻게 고쳤는지 타임라인으로 작성해라.
```

```
명령어 히스토리를 보고, 진행 상황과 검증 신호를 연대기 순으로 나열해라.
```