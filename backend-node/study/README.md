# Study Workspace

이 디렉터리는 현재 저장소의 활성 학습 경로를 담는다.

## 활성 트랙

- `Node-Backend-Architecture`

## 사용 원칙

- `legacy/`는 참조용이고, 실제 학습과 구현은 `study/`에서 진행한다.
- 프로젝트 상태는 `planned`, `in-progress`, `verified`, `reference-only`로 표시한다.
- `notion/`은 로컬 전용 기술 노트이며 Git에 포함하지 않는다.

