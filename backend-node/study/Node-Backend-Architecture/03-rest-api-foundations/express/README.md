# Express Implementation

## 문제 범위

도서 CRUD API를 Router -> Controller -> Service 구조로 구현한다.

## 실행

- install: `pnpm install`
- build: `pnpm run build`
- test: `pnpm run test`

## 현재 상태

- 상태: `verified`
- 원본: `legacy/01-rest-api/express-impl/solve/solution`
- 새 경로 검증: 완료

## 구현 메모

수동 DI와 composition root를 직접 드러내는 레인이다.
