/**
 * Book type definition.
 *
 * TODO: Define the full Book interface with these fields:
 *   - id: string (UUID)
 *   - title: string
 *   - author: string
 *   - publishedYear: number
 *   - genre: string
 *   - price: number
 *
 * Also define a CreateBookDto (omitting `id`) and UpdateBookDto (Partial of CreateBookDto).
 */

export interface Book {
  // TODO: Define properties
}

export type CreateBookDto = unknown; // TODO: Replace with proper type
export type UpdateBookDto = unknown; // TODO: Replace with proper type
