/**
 * Book Router — Route definitions only.
 *
 * TODO:
 *   1. Create an Express Router.
 *   2. Map HTTP methods + paths to BookController methods.
 *   3. Wrap each handler with asyncHandler.
 *   4. No business logic here — only routing.
 */

import { Router } from "express";

export function createBookRouter(/* TODO: accept controller */): Router {
  const router = Router();

  // TODO: Define routes
  // router.get("/", ...)
  // router.get("/:id", ...)
  // router.post("/", ...)
  // router.put("/:id", ...)
  // router.delete("/:id", ...)

  return router;
}
