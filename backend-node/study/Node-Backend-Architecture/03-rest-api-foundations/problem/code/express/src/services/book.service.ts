/**
 * BookService — Business logic and data access layer.
 *
 * TODO:
 *   1. Store books in an in-memory Map<string, Book>.
 *   2. Implement: findAll(), findById(), create(), update(), delete().
 *   3. Generate UUIDs using crypto.randomUUID().
 *   4. This class must NOT know about Express (no req/res).
 */

export class BookService {
  // TODO: Implement
}
