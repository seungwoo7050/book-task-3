/**
 * BookController — HTTP request/response handling layer.
 *
 * TODO:
 *   1. Receive BookService via constructor injection.
 *   2. Implement handler methods for each CRUD operation.
 *   3. Extract data from req (params, body) and delegate to the service.
 *   4. Send appropriate HTTP responses (status codes + JSON).
 */

export class BookController {
  // TODO: Implement — inject BookService via constructor
}
