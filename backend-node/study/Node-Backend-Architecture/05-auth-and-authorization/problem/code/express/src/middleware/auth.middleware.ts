/**
 * TODO: Implement auth middleware
 *   1. Extract Bearer token from Authorization header.
 *   2. Verify the JWT.
 *   3. Attach decoded payload to req.user.
 *   4. Call next() on success, respond 401 on failure.
 */
