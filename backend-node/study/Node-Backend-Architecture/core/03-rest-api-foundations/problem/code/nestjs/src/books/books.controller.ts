/**
 * BooksController - /books의 HTTP 요청을 처리한다.
 *
 * TODO:
 *   1. @Controller("books") decorator를 사용한다.
 *   2. constructor로 BooksService를 주입한다.
 *   3. @Get, @Post, @Put, @Delete로 handler를 구현한다.
 *   4. @Param("id"), @Body()로 요청 데이터를 추출한다.
 */
