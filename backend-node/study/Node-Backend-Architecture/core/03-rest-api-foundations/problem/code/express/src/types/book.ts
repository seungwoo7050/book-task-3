/**
 * Book 타입 정의.
 *
 * TODO: 다음 필드를 가진 전체 Book interface를 정의한다:
 *   - id: string (UUID)
 *   - title: string
 *   - author: string
 *   - publishedYear: number
 *   - genre: string
 *   - price: number
 *
 * `id`를 제외한 CreateBookDto와 CreateBookDto의 Partial인 UpdateBookDto도 정의한다.
 */

export interface Book {
  // TODO: 속성을 정의한다
}

export type CreateBookDto = unknown; // TODO: Replace with proper type
export type UpdateBookDto = unknown; // TODO: Replace with proper type
