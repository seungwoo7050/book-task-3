/**
 * BookService - 비즈니스 로직과 데이터 접근 계층을 담당한다.
 *
 * TODO:
 *   1. 책을 in-memory Map<string, Book>에 저장한다.
 *   2. findAll(), findById(), create(), update(), delete()를 구현한다.
 *   3. crypto.randomUUID()로 UUID를 생성한다.
 *   4. 이 클래스는 Express를 알면 안 된다(req/res 금지).
 */

export class BookService {
  // TODO: 구현한다
}
