/**
 * BookController - HTTP 요청/응답 처리 계층이다.
 *
 * TODO:
 *   1. constructor injection으로 BookService를 받는다.
 *   2. 각 CRUD 동작에 대한 handler 메서드를 구현한다.
 *   3. req(params, body)에서 데이터를 꺼내 service에 위임한다.
 *   4. 적절한 HTTP 응답(status code + JSON)을 보낸다.
 */

export class BookController {
  // TODO: 구현한다 — inject BookService via constructor
}
