# BOJ 1149 — 개발 타임라인 (후반)

## Phase 2
### Session 3
- 목표: edge case를 점검한다.
- 진행: N=1이면 for 루프에 안 들어가고 `min(prev)` = 첫 집의 세 비용 중 최소를 출력한다.
- 이슈: 모든 비용이 같으면? 어떤 색이든 같은 최소 비용이 나온다. 자동 처리.

### Session 4
- 검증: fixture 통과.

CLI:

```bash
$ make -C study/Core-08-DP/1149/problem test
```

```text
Test 1: PASS
Results: 1/1 passed, 0 failed
```

### Session 5
- 정리:
  - 이 문제는 DP 상태를 "집 번호 × 색상"으로 유지하는 기본 패턴이다.
  - 그리디가 안 되는 이유를 직접 반례로 확인한 게 도움이 됐다. [1,100,100], [100,1,100], [100,100,1] 같은 입력에서 그리디(매번 최소 선택)는 최적이 아니다.
  - 공간 최적화 패턴(이전 행만 유지)은 0/1 배낭과 같은 아이디어다. DP 트랙에서 이 패턴이 반복된다.
