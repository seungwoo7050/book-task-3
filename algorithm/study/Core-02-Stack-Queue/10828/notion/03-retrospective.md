# 회고 — 인터페이스를 직접 만들어 보는 경험

## 이 문제가 남긴 것

스택의 5가지 연산을 일일이 구현하면서, "이 연산들이 왜 O(1)인가"를 체감했다.
Python list의 `append()`와 `pop()`이 amortized O(1)이라는 사실이 스택을 리스트로 구현 가능하게 만드는 근거다.

출력을 모아서 한 번에 내보내는 패턴은 이후에도 계속 사용한다.

## 다음에 적용할 것
- 결과를 리스트에 모아서 `'\n'.join()`으로 출력하는 패턴 유지
