# BOJ 2748 — 디버깅 기록

## 함정: n=0 처리

**증상**: $n = 0$일 때 1 출력

**원인**: `a, b = 0, 1` 후 루프에 진입하지 않으면 `b = 1`이 출력

**해결**: `if n <= 1: print(n); return` 으로 기저 케이스 먼저 처리

## 확인 과정

```bash
make -C problem test
```

$n = 0, 1, 10, 90$ 통과.
