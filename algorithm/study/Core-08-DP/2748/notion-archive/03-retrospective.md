# BOJ 2748 — 회고

## 배운 것

피보나치는 DP의 "hello world"다. 순수 재귀의 $O(2^n)$을 $O(n)$으로 줄이는 것은 **중복 계산 제거**의 위력을 보여준다. 이 원칙이 1149(RGB 거리)와 12865(배낭 문제)에서 더 복잡한 형태로 등장한다.

## 공간 최적화

배열 없이 변수 두 개로 해결한 것은 "현재 상태가 이전 두 상태에만 의존"하기 때문. 1149에서도 같은 원리로 2D 배열을 1D로, 나아가 변수 3개로 최적화할 수 있다.
