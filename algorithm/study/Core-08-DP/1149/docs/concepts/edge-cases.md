# 경계 사례 점검 — BOJ 1149 RGB Street

## 1. N = 2
두 집만 → 3×3 = 9 조합 중 인접 다른 색 → 최소.

## 2. 한 색이 항상 최저
그리디로는 실패 (인접 제약). DP 필요.

## 3. 모든 비용이 동일
어떤 색이든 같은 답.

## 핵심 주의점
- `min(prev[1], prev[2])` 등 인접 색 제외 로직
- 마지막 `min(prev)` 로 세 색 중 최소 선택
