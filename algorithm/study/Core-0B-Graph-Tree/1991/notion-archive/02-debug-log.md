# BOJ 1991 — 디버깅 기록

## 주의점 1: 빈 노드 표현

`.`을 빈 자식으로 사용. 재귀 베이스 케이스에서 `if node == '.'` 필수.

## 주의점 2: result 리스트 재활용

세 순회마다 `result.clear()` 호출. 잊으면 이전 순회 결과가 섞임.

## 주의점 3: 루트 노드

항상 `A`가 루트. 문제 조건.

## 확인 과정

```bash
make -C problem test
```

PASS.
