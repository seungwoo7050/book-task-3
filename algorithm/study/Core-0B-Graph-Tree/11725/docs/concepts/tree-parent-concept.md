# 트리 부모 찾기 개념 정리

## CLRS 연결
CLRS Ch 22.2 BFS — 루트에서 BFS하면 각 노드의 부모를 자연스럽게 기록.

## 핵심 아이디어
무방향 그래프로 주어진 트리를 루트(1)에서 BFS/DFS로 탐색하면
각 노드를 처음 방문할 때의 이전 노드가 부모.

## 알고리즘
1. 인접 리스트 구성
2. BFS(1) 시작
3. 미방문 인접 노드 `v`에 대해 `parent[v] = u` 기록

## 복잡도
$O(N)$ — 트리의 간선 수 = $N-1$.
