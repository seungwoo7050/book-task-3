# Edge Cases — BOJ 1197 MST

## 1. 음의 가중치
MST에서는 음의 가중치도 유효 — 가능한 한 선택.

## 2. 같은 가중치 간선 여러 개
정렬 후 아무거나 선택 — MST는 유일하지 않을 수 있음.

## 3. V = 1
간선 없음. 답 = 0.

## 핵심 주의점
- 간선 가중치 범위 $|C| \le 10^6$ → `int` 범위 내
- `V-1`개 선택 시 조기 종료 가능
