# BOJ 11279 — 회고

## 배운 것

Python `heapq`의 제한(최소 힙 전용)과 이를 우회하는 부호 반전 트릭. 1927(최소 힙)과 쌍으로 연습하면 힙 기본기를 다질 수 있다.

## I/O 최적화

`sys.stdin.readline` + 출력 버퍼링은 이 단계에서 습관이 되어야 한다. 특히 연산 수가 $10^5$ 이상이면 `print` 반복은 TLE 위험.
