# 키로거 — 에디터의 확장판

## 문제를 처음 읽었을 때

BOJ 5397은 키 입력 로그에서 실제 비밀번호를 복원하는 문제다.
`<`(왼쪽 이동), `>`(오른쪽 이동), `-`(백스페이스), 그 외(문자 삽입)를 처리해야 한다.

1406(에디터)을 풀었다면 즉시 알아챈다: **이건 같은 문제의 변형이다.**
차이점은 여러 테스트 케이스가 있고, 총 키 입력 길이가 최대 $5 \times 10^6$이라는 것이다.

## 문제의 핵심 구조

- **입력**: $T$개의 테스트 케이스, 각 줄에 키 입력 문자열 (총 길이 $\le 5 \times 10^6$)
- **출력**: 각 테스트 케이스의 최종 비밀번호
- **핵심**: 1406과 동일한 Two-Stack 패턴이지만, 출력 성능에도 신경 써야 함

## 왜 이 문제를 골랐는가

1406에서 배운 Two-Stack 패턴을 **재적용**하는 연습이다.
"한 번 배운 패턴을 다른 문제에서 인식하고 사용하는" 능력은 별도로 훈련해야 한다.
또한 대량 출력 시 `sys.stdout.write`를 사용해야 하는 상황을 경험하게 된다.
