#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    // 할 일: N, M, R과 배열을 읽는다
    // 할 일: 레이어로 분해하고 각 레이어를 회전한 뒤 다시 조립한다

    return 0;
}
