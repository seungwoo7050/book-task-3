# 지식 인덱스

> 프로젝트: 팰린드롬인지 확인하기
> 아래 내용은 `notion-archive/04-knowledge-index.md`에 남아 있던 기존 학습 기록을 현재 공개 노트 기준으로 다듬은 버전이다.

## 핵심 개념

### 회문 (Palindrome)
앞에서 읽으나 뒤에서 읽으나 같은 문자열.
수학적으로는 $s[i] = s[n-1-i]$ ($\forall i \in [0, \lfloor n/2 \rfloor)$).
단일 문자와 빈 문자열은 관례적으로 회문이다.

### 슬라이싱 vs 투 포인터
- **슬라이싱**(`s[::-1]`): Python 전용, O(n) 공간, 가독성 최고
- **투 포인터**: 언어 무관, O(1) 공간, 조기 종료 가능

이 문제에서는 슬라이싱을 택했지만, 두 가지를 모두 알고 있어야 상황에 맞는 선택을 할 수 있다.

### sys.stdin.readline
Python의 `input()`은 내부적으로 프롬프트 처리 등의 오버헤드가 있다.
`sys.stdin.readline()`은 그런 과정을 생략하고 직접 읽기 때문에, 대량 입력에서 유의미하게 빠르다.
단, 끝에 개행 문자가 포함되므로 `.strip()`이 필수다.

## 연결되는 문제들

| 개념 | 관련 문제 / 주제 |
|------|-----------------|
| 문자열 뒤집기 | 스택으로 문자열 뒤집기 (Core-02) |
| 투 포인터 | 두 수의 합 문제, 정렬된 배열에서의 탐색 |
| 회문 확장 | Manacher 알고리즘, 최장 회문 부분 수열 (DP) |

## CLRS 매핑

| 챕터 | 연관성 |
|------|--------|
| Ch 2 | 루프 불변식으로 비교 기반 알고리즘의 정확성 증명 |
| Ch 3 | O(n) vs O(n²) 회문 검사의 점근 분석 |

## 다시 연결해 볼 질문

- `팰린드롬인지 확인하기`에서 쓴 개념이 같은 트랙의 다음 문제에서 어떻게 더 어려워지는가?
- `작은 입력을 안정적으로 읽고, 조건 분기를 코드와 문서로 함께 정리하는 감각`를 다른 자료구조나 다른 언어 구현으로 옮기면 무엇이 달라지는가?
- 이번에 정리한 용어와 판단 기준을 README 한 단락으로 축약하면 무엇이 남는가?

## 한 줄 정리 후보

- `팰린드롬인지 확인하기`를 다시 설명할 때는 문제 이름보다 `작은 입력을 안정적으로 읽고, 조건 분기를 코드와 문서로 함께 정리하는 감각`를 먼저 말한다.
- 개념 이름만 적지 말고, 그 개념이 어떤 입력이나 어떤 전이 규칙에서 필요했는지를 같이 남긴다.

## 다음 문제와 연결되는 포인트

- 다음 프로젝트: [`../../11053/README.md`](../../11053/README.md) (가장 긴 증가하는 부분 수열)
- 같은 트랙의 다음 문제로 갈수록 자료구조 선택과 검증 순서가 더 중요해진다. 그래서 `05-development-timeline.md`에 남긴 실행 흐름도 함께 기억해 두는 편이 좋다.

## 바로 열어 볼 문서

- [`edge-cases.md`](../docs/concepts/edge-cases.md)
- [`palindrome-concept.md`](../docs/concepts/palindrome-concept.md)
- [`../docs/references/approach.md`](../docs/references/approach.md)
- [`../docs/references/reproducibility.md`](../docs/references/reproducibility.md)
- [`05-development-timeline.md`](05-development-timeline.md)
