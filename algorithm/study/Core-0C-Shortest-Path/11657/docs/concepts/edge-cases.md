# 경계 사례 점검 — BOJ 11657 Time Machine

## 1. 음의 사이클
$V$번째 반복에서 완화 발생 → `-1` 출력.

## 2. 도달 불가
`dist[i] == INF` → `-1` 출력.

## 3. 음의 가중치이나 사이클 없음
정상 최단 경로 출력.

## 핵심 주의점
- `dist[a] != INF` 체크 필수 — 도달 불가 노드에서 완화 방지
- 출력에서 "음의 사이클" vs "도달 불가" 구분: 둘 다 `-1`이지만 사이클이면 전체 `-1`
