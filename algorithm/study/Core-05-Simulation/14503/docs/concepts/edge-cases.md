# 경계 사례 점검 — BOJ 14503 Robot Vacuum

## 1. 시작 위치가 벽으로 둘러싸임
1칸만 청소 가능. 출력: 1.

## 2. 전체가 빈 칸
가능한 모든 칸을 청소.

## 3. 후진만으로 이동
4방향 모두 청소됨 → 후진 반복.

## 핵심 주의점
- `cleaned` 배열과 `grid`(벽) 분리
- 후진 시 방향 변경 없음
- 후진 시 벽만 체크 (청소된 칸으로는 후진 가능)
