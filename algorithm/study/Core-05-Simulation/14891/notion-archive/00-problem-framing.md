# BOJ 14891 — 톱니바퀴: 연쇄 반응 시뮬레이션

## 첫인상

4개의 톱니바퀴가 맞물려 있고, 하나를 돌리면 인접한 톱니바퀴가 연쇄적으로 반대 방향으로 회전할 수 있다. "연쇄 반응"을 정확히 시뮬레이션하는 것이 핵심이다. 규칙 자체는 단순하지만, **전파 순서**를 잘못 처리하면 결과가 틀어진다.

## 문제 구조 파악

- 4개의 톱니바퀴, 각각 8개의 톱니 (N극 0, S극 1)
- 톱니바퀴의 오른쪽 접점은 인덱스 2, 왼쪽 접점은 인덱스 6
- 하나를 회전시키면, 인접한 톱니바퀴의 접점이 다를 경우 반대 방향으로 회전
- K번의 회전 명령 후 점수 계산: `gears[i][0] * 2^i`의 합

핵심: **회전을 적용하기 전에** 모든 전파 방향을 먼저 결정해야 한다. 회전 후에 접점을 비교하면 이미 값이 바뀌어 있다.

## 왜 이 문제를 선택했는가

단순한 규칙의 연쇄적 적용이 복잡한 동작을 만들어내는 시뮬레이션의 대표 사례. deque 자료구조와 회전 연산의 실전 활용을 훈련한다.

## 난이도 평가

Silver 등급. 로직은 이해하기 쉽지만, 전파 순서와 회전 적용 순서를 분리하는 것이 실수 포인트.
