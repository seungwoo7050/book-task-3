# BOJ 1181 — 디버깅 기록

## 함정 1: 중복 제거 누락

**증상**: 같은 단어가 여러 번 출력됨

**원인**: `set`으로 변환하지 않고 리스트로 정렬

**해결**: 입력 시 `set()` 사용으로 중복 자동 제거

## 함정 2: 입력 줄바꿈

**증상**: 단어 비교가 잘못됨

**원인**: `input()` 대신 `sys.stdin.readline()`을 쓸 때 `.strip()`을 빠뜨리면 `\n`이 포함됨

**해결**: `.strip()` 호출

## 확인 과정

```bash
make -C problem test
```

PASS.
