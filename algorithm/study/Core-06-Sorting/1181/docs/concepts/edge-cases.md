# Edge Cases — BOJ 1181 Word Sort

## 1. 모든 단어가 같음
`set`으로 중복 제거 → 1개만 출력.

## 2. 같은 길이의 다른 단어
사전순 정렬 확인. `"abc"` < `"abd"`.

## 3. 단어 길이가 모두 다름
길이순 정렬만 적용.

## 4. 빈 문자열
문제 조건상 없음 (길이 ≥ 1).

## 핵심 주의점
- `input().strip()` 필수 — 개행 문자 제거
- `set`은 해시 기반이므로 삽입 순서 비보장 → 반드시 정렬 필요
