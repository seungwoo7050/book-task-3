# Orientation as a Primitive

## CLRS Connection

CLRS Ch 33 builds many planar algorithms from orientation tests and cross products.

## Study Notes

- 선분 교차 판정과 convex hull 모두 `ccw` 계산이 핵심 primitive다.
- hull 출력 순서를 deterministic하게 정하면 fixture 검증이 쉬워진다.
- collinear points 처리 규칙을 먼저 정해야 테스트가 안정된다.
