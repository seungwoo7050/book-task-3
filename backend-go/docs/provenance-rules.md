# Provenance Rules

## Source Labels

- `Legacy source`: `legacy/`에서 가져온 문제나 구현
- `Study-authored`: `study/`에서 새로 작성한 문제, 문서, 구현
- `Local-only`: `notion/`, 개인 메모, 임시 검증 기록

## Claims

- 재현 가능한 명령이 있으면 그대로 적는다.
- 아직 검증하지 못한 내용은 추정으로 표시하지 않고 `Known gaps`에 둔다.
- 존재하지 않는 파일이나 경로를 README에서 참조하지 않는다.

