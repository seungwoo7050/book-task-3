# 01 Go Syntax And Tooling Series Map

`00-go-fundamentals/01-go-syntax-and-tooling`는 Go 문법과 `go run` / `go test` 루프를 가장 작은 문자열 처리 과제로 익히는 시작점이다.

## 이 시리즈가 복원하는 것

- 시작점: 변수, 함수, 제어 흐름을 실제 코드로 익혀야 한다.
- 구현 축: 문자열을 단어 단위로 정규화하는 라이브러리와 요약 CLI를 `solution/go`에 구현했다.
- 검증 축: 2026-03-07 기준 `go run ./cmd/toolingdemo`가 정상 실행됐다.
- 글 수: 1편

## 읽는 순서

- [10-first-go-run-loop.md](10-first-go-run-loop.md)

## 근거 기준

- 소스코드, README, docs, 테스트, CLI만 입력 근거로 사용했다.
- 기존 blog 초안과 `_legacy` 본문은 입력 근거로 사용하지 않았다.
