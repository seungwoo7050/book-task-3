# Go Implementation

- Scope: proto-first contract, minimal server/client, interceptor examples
- Build: `mkdir -p ./bin && go build -o ./bin/server ./server/cmd && go build -o ./bin/client ./client/cmd`
- Test: `go test ./...`
- Status: `verified`
- Known gaps: generated `.pb.go` workflow는 아직 자동화하지 않았다
