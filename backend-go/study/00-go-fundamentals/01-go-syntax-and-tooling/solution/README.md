# Solution

## 답안 요약

- 구현 위치: `solution/go`
- 핵심 범위: 문자열 정규화, 단어 빈도 계산, 요약 CLI
- 이 답안은 `verified` 상태 기준으로 공개 표면을 정리했다.

## 구현 진입점

- `cd solution/go && go run ./cmd/toolingdemo`
- `cd solution/go && go test ./...`

## 현재 한계

- stdin/flag 처리는 아직 포함하지 않는다.
