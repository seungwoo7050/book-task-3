# Go Implementation

- Scope: API server, purchase service, relay, repository, e2e tests
- Build: `mkdir -p ./bin && go build -o ./bin/api ./cmd/api`
- Test: `go test ./...`
- Runtime verification: `make repro`
- Status: `verified`
