# Go Implementation

- Scope: worker pool, pipeline, context-aware cancellation
- Build: `go run ./cmd/workerpool` / `go run ./cmd/pipeline`
- Test: `go test -race ./...`
- Status: `verified`
- Known gaps: distributed queue와 연결하지 않음

