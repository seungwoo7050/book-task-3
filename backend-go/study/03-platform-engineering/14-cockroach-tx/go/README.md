# Go Implementation

- Scope: repository, service, retry helper, HTTP purchase handler
- Build: `mkdir -p ./bin && go build -o ./bin/server ./cmd/server`
- Test: `go test ./...`
- Runtime verification: `make repro`
- Status: `verified`
