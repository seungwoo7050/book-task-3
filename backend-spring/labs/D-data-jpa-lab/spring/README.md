# D-data-jpa-lab Spring

- Status: `verified scaffold`
- Problem scope covered: JPA CRUD, pagination, optimistic locking, search-ready structure
- Commands:
  - `cp .env.example .env`
  - `make run`
  - `make lint`
  - `make test`
  - `make smoke`
  - `docker compose up --build`
