# C-authorization-lab series map

인증과 분리된 authorization을 membership lifecycle로 읽게 만드는 시리즈다. macOS + VSCode 통합 터미널 기준으로 `spring/` 워크스페이스를 따라가면 된다.

## 읽는 순서

1. [10-development-timeline.md](10-development-timeline.md): organization, invite, membership, role 변경이 어떤 순서로 고정됐는지 따라간다.

## 이 시리즈가 답하는 질문

- 왜 method security보다 service logic을 먼저 택했는가
- membership lifecycle로 보면 무엇이 더 선명해지는가
- 아직 일부러 남겨 둔 인가 범위는 무엇인가
