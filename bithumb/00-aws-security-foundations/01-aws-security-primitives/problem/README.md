# Problem

이 과제는 AWS IAM 문서를 통째로 구현하려는 것이 아니다. 주니어가 가장 먼저 헷갈리는
`Effect`, `Action`, `Resource`, `explicit deny` 네 가지를 코드로 체감하는 것이 목표다.

입력:
- policy JSON
- request JSON

출력:
- 허용/거부 결정
- 어떤 statement가 적용됐는지 설명

