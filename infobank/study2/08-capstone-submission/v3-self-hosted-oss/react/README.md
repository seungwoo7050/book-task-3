# React Frontend

이 디렉터리는 `v3-self-hosted-oss` 운영자 UI를 담는다.

## Scope

- login gate
- datasets upload
- knowledge base upload
- jobs queue / selection
- run-scoped overview
- run-scoped failures
- run-scoped session review

## Commands

```bash
pnpm install
pnpm test --run
pnpm build
pnpm dev
```
