# Problem

`v3-self-hosted-oss`의 목표는 `v2`를 single-team self-hosted QA Ops 도구로 올리는 것이다.

필수 요구:

- single admin auth
- transcript JSONL import
- Markdown ZIP KB import
- 비동기 evaluation job
- run-scoped dashboard / session review
- Docker Compose quickstart

범위 밖:

- multi-tenant
- RBAC / SSO
- live chatbot serving
- billing / hosted SaaS
