# Node Implementation

이 디렉터리는 `v1` 실시간 개입 backend를 담는 자리다.

## Planned Scope

- intervention timing engine
- action generation adapter
- material retrieval orchestration
- live meeting state management
