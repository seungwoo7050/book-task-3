# Go Implementation

- 상태: `verified`
- 범위: shard router, replica groups, disk-backed per-node store, follower catch-up

## Commands

```bash
GOWORK=off go test ./...
GOWORK=off go run ./cmd/clustered-kv
```
