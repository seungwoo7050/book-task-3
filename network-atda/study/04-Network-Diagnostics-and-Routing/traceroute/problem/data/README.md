# Traceroute Data

This project does not ship a fixed capture or topology fixture. The live implementation sends probes to the host selected at runtime, while the automated verification relies on synthetic packets built inside `python/tests/`.
