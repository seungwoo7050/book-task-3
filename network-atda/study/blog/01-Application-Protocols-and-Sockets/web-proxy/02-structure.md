# Web Proxy 구조 메모

- `00-series-map.md`: proxy가 떠안는 역할을 먼저 정리한다.
- `10-development-timeline.md`: request 수신 -> cache 판단 -> origin fetch -> cache 저장 -> 응답 반환 순서를 따라간다.
- `01-evidence-ledger.md`: URL parsing, cache key, 오류 응답 근거를 짧게 남긴다.

이번 재작성에서는 "캐시 서버"보다 "중개자 구조"를 더 앞세웠다.
