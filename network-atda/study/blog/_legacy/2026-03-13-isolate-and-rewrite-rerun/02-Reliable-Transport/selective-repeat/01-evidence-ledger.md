# Evidence Ledger — Selective Repeat

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | Session 1 | window 전체가 아니라 packet별 ACK 상태를 추적하는 sender를 만든다. | `python/src/selective_repeat.py::selective_repeat_send_receive`의 sender 상태 | Selective Repeat의 핵심은 `send_base`보다 `acked`와 `timers`를 별도로 두는 것이다. | outstanding packet마다 timer를 두고, timeout이 난 sequence만 재전송하도록 sender 상태를 분리했다. | `make -C study/02-Reliable-Transport/selective-repeat/problem run-solution` | loss/corruption 설정에서도 특정 sequence만 골라 재전송할 수 있는 표면이 생겼다. | `python/src/selective_repeat.py::selective_repeat_send_receive` | window protocol에서 상태 공간이 늘어나는 지점은 sender보다 ACK bookkeeping이다. | receiver 쪽에도 buffer와 in-order delivery 규칙을 붙인다. |
| 2 | Session 2 | window 안에 먼저 도착한 packet을 버리지 않고, `recv_base`가 열릴 때만 delivery한다. | `python/src/selective_repeat.py::selective_repeat_send_receive`의 receiver 상태 | GBN과 달리 out-of-order packet을 저장해야 selective retransmission의 이점이 살아난다. | `recv_buffer`에 packet을 넣고, 중복 old packet에는 re-ACK, window 밖 packet에는 ignore 규칙을 추가했다. | `make -C study/02-Reliable-Transport/selective-repeat/problem run-solution` | buffered seq와 delivered seq 로그가 분리되면서 receiver 동작이 눈에 보이기 시작했다. | `python/src/selective_repeat.py::selective_repeat_send_receive` | Selective Repeat는 송신측 timer만 늘리는 게 아니라 수신측 reorder buffer까지 책임져야 완성된다. | fixture와 test harness로 전체 전달 조건을 고정한다. |
| 3 | Session 3 | 데모 데이터와 자동 검증을 같은 entrypoint에 묶어 chronology를 재현 가능하게 만든다. | `problem/script/test_selective_repeat.sh`, `python/tests/test_selective_repeat.py` | transfer 성공만 보지 말고 fixture 존재 여부와 무손실 경로도 같이 잠가야 한다. | `load_messages()`로 payload source를 고정하고, test에서 전달 완료와 fixture availability를 따로 검증했다. | `make -C study/02-Reliable-Transport/selective-repeat/problem test` | Selective Repeat Test Suite: 전체 전달 PASS, selective retransmit 확인 PASS | `python/tests/test_selective_repeat.py` | 패킷별 timer 관리 | 다음 track에서는 packet을 보내는 쪽이 아니라 trace를 읽는 쪽 chronology로 넘어간다. |
