# Evidence Ledger — RDT Protocol

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | Session 1 | loss/corrupt channel에서도 한 번에 한 packet만 확실히 넘기는 baseline을 만든다. | `python/src/rdt3.py::rdt_send_receive`, `problem/code/packet.py`, `problem/code/channel.py` | reliable transfer의 최소 단위는 alternating-bit와 timeout 조합일 것이다. | 첫 packet 전송, ACK 확인, duplicate/손상 packet에서 이전 ACK 재전송, timeout 재전송을 하나의 event loop로 묶었다. | `make -C study/02-Reliable-Transport/rdt-protocol/problem run-solution-rdt3` | rdt3 demo surface가 생기면서 sender/receiver 로그를 sequence 단위로 읽을 수 있게 됐다. | `python/src/rdt3.py::rdt_send_receive` | sequence number 하나만 있어도 duplicate와 ACK loss를 구분할 수 있다. | pipeline을 열고 cumulative ACK로 window를 움직인다. |
| 2 | Session 2 | 여러 packet이 outstanding 상태인 동안 base, next_seq, timer가 어떻게 움직이는지 코드로 설명한다. | `python/src/gbn.py::gbn_send_receive` | cumulative ACK를 받으면 base만 앞으로 밀고 timeout 시 base..next_seq-1 전체를 다시 보내면 된다. | packet prebuild, sliding window 송신, out-of-order 수신 시 마지막 정상 ACK 재전송, timeout 전체 재전송을 구현했다. | `make -C study/02-Reliable-Transport/rdt-protocol/problem run-solution-gbn` | GBN demo가 loss/corruption 환경에서도 전체 메시지를 끝까지 전달했다. | `python/src/gbn.py::gbn_send_receive` | GBN의 단순함은 수신측 버퍼를 포기하고 송신측 재전송 범위를 넓게 잡는 데서 나온다. | 두 프로토콜을 같은 harness에서 재검증하고 차이를 문서화한다. |
| 3 | Session 3 | rdt3와 GBN을 같은 test entrypoint에서 재현하고, SR과의 경계도 설명한다. | `problem/script/test_rdt.sh`, `python/tests/test_rdt.py`, `docs/concepts/gbn-vs-sr.md` | CLI가 sequence 로그를 그대로 보여 줘야 chronology도 과장되지 않는다. | canonical test를 고정하고 packet utility test와 비교 문서를 함께 남겼다. | `make -C study/02-Reliable-Transport/rdt-protocol/problem test` | RDT Protocol Test Suite: RDT 3.0 transfer PASS, GBN transfer PASS | `python/tests/test_rdt.py` | alternating bit와 cumulative ACK의 차이 | 다음 단계에서는 receiver buffer와 packet별 timer가 필요한 Selective Repeat로 넘어간다. |
