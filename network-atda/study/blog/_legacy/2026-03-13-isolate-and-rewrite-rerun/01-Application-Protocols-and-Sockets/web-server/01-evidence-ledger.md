# Evidence Ledger — Web Server

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | Session 1 | HTTP request line을 filename으로 바꾸고 200/404 응답을 직접 만드는 가장 작은 정적 서버를 설명하려면 먼저 실행 진입점이 흔들리지 않아야 했다. | `python/src/web_server.py::main` | README보다 먼저 실제 entrypoint가 어떤 입력과 출력 surface를 갖는지 잡아야 한다. | 핵심 entrypoint인 `python/src/web_server.py::main`를 기준으로 socket/event loop 또는 main flow를 먼저 닫았다. | `make -C study/01-Application-Protocols-and-Sockets/web-server/problem run-solution` | run surface가 정리되면서 이후 판단을 함수 단위로 이어 붙일 수 있게 됐다. | `python/src/web_server.py::main` | HTTP 요청 라인의 최소 파싱 규칙 | protocol/algorithm 핵심 로직을 파일 안쪽 helper로 채운다. |
| 2 | Session 2 | HTTP request line을 filename으로 바꾸고 200/404 응답을 직접 만드는 가장 작은 정적 서버 | `python/src/web_server.py::handle_client` | 이 프로젝트의 핵심은 설명문이 아니라 request/reply, timer, cache, parser 같은 작은 branch에 숨어 있다. | `python/src/web_server.py::handle_client`를 중심으로 프로젝트 고유 규칙을 구현하고, 성공/실패 branch가 어디서 갈리는지 드러냈다. | `make -C study/01-Application-Protocols-and-Sockets/web-server/problem run-solution` | 핵심 로직이 눈에 보이는 함수 단위로 정리되면서 test가 기대하는 입출력과 연결됐다. | `python/src/web_server.py::handle_client` | 정적 파일 서빙과 `Content-Type` 결정 | 자동 검증과 개념 문서로 경계와 한계를 정리한다. |
| 3 | Session 3 | 코드가 설명하는 계약을 CLI와 test 결과로 다시 확인한다. | `python/tests/test_web_server.py`, `make -C study/01-Application-Protocols-and-Sockets/web-server/problem test`, `docs/concepts/content-types.md` | 좋은 chronology는 마지막에 test result와 남은 한계를 같이 남겨야 과장되지 않는다. | canonical test를 다시 돌리고, docs/concepts와 현재 한계를 함께 묶어 마감했다. | `make -C study/01-Application-Protocols-and-Sockets/web-server/problem test` | Web Server Test Suite: `GET /hello.html` 200, `GET /nonexistent` 404, 총 3 passed | `python/tests/test_web_server.py` | HTTP 요청 라인의 최소 파싱 규칙과 정적 파일 서빙 및 `Content-Type` 결정 | 다음 글에서는 더 넓은 protocol surface 또는 다음 track으로 이어진다. |
