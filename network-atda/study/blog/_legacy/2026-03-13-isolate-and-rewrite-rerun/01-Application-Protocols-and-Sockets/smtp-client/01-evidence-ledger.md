# Evidence Ledger — SMTP Client

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | Session 1 | 220/250/354/221 reply code를 확인하며 DATA 종료 마커까지 직접 보내는 SMTP 대화를 설명하려면 먼저 실행 진입점이 흔들리지 않아야 했다. | `python/src/smtp_client.py::main` | README보다 먼저 실제 entrypoint가 어떤 입력과 출력 surface를 갖는지 잡아야 한다. | 핵심 entrypoint인 `python/src/smtp_client.py::main`를 기준으로 socket/event loop 또는 main flow를 먼저 닫았다. | `make -C study/01-Application-Protocols-and-Sockets/smtp-client/problem run-solution` | run surface가 정리되면서 이후 판단을 함수 단위로 이어 붙일 수 있게 됐다. | `python/src/smtp_client.py::main` | 3자리 SMTP 응답 코드에 따른 제어 흐름 | protocol/algorithm 핵심 로직을 파일 안쪽 helper로 채운다. |
| 2 | Session 2 | 220/250/354/221 reply code를 확인하며 DATA 종료 마커까지 직접 보내는 SMTP 대화 | `python/src/smtp_client.py::send_command` | 이 프로젝트의 핵심은 설명문이 아니라 request/reply, timer, cache, parser 같은 작은 branch에 숨어 있다. | `python/src/smtp_client.py::send_command`를 중심으로 프로젝트 고유 규칙을 구현하고, 성공/실패 branch가 어디서 갈리는지 드러냈다. | `make -C study/01-Application-Protocols-and-Sockets/smtp-client/problem run-solution` | 핵심 로직이 눈에 보이는 함수 단위로 정리되면서 test가 기대하는 입출력과 연결됐다. | `python/src/smtp_client.py::send_command` | `CRLF`와 `DATA` 종료 구분자 처리 | 자동 검증과 개념 문서로 경계와 한계를 정리한다. |
| 3 | Session 3 | 코드가 설명하는 계약을 CLI와 test 결과로 다시 확인한다. | `python/tests/test_smtp_client.py`, `make -C study/01-Application-Protocols-and-Sockets/smtp-client/problem test`, `docs/concepts/email-format.md` | 좋은 chronology는 마지막에 test result와 남은 한계를 같이 남겨야 과장되지 않는다. | canonical test를 다시 돌리고, docs/concepts와 현재 한계를 함께 묶어 마감했다. | `make -C study/01-Application-Protocols-and-Sockets/smtp-client/problem test` | SMTP Client Test Suite: 전체 SMTP dialogue 완료, 성공 메시지와 대화 로그 확인, 총 3 passed | `python/tests/test_smtp_client.py` | 3자리 SMTP 응답 코드에 따른 제어 흐름 and `CRLF`와 `DATA` 종료 구분자 처리 | 다음 글에서는 더 넓은 protocol surface 또는 다음 track으로 이어진다. |
