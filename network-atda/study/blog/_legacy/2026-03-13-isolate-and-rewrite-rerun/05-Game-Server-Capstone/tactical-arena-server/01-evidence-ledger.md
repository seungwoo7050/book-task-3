# Evidence Ledger — Tactical Arena Server

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | Session 1 | line-based control message와 binary input/snapshot packet이 같은 서버에서 공존할 수 있는 wire surface를 정한다. | `cpp/src/protocol.cpp`, `docs/concepts/protocol.md`, `problem/code/control-protocol.txt` | capstone의 첫 복잡성은 game rule보다 protocol surface가 TCP/UDP 두 채널로 갈라진다는 점이다. | control line parse/format, resume token 생성, binary packet encode/decode를 별도 protocol layer로 분리했다. | `make -C study/05-Game-Server-Capstone/tactical-arena-server/problem run-server` | control command와 UDP input packet을 같은 naming/field 규칙으로 설명할 수 있게 됐다. | `cpp/src/protocol.cpp::parse_control_line` and `cpp/src/protocol.cpp::decode_input_packet` | capstone에서는 application protocol 설계가 곧 테스트 가능성의 출발점이다. | fixed tick simulation과 reconnect/forfeit 상태 전이를 구현한다. |
| 2 | Session 2 | input submit, projectile update, respawn, forfeit timeout, snapshot build를 deterministic step 함수에 모은다. | `cpp/src/state.cpp`, `docs/concepts/simulation.md` | room strand 안에서 돌아가는 authoritative state는 `step()`이 하나의 truth가 되어야 설명 가능하다. | submit_input, step, build_snapshot_packet, apply_forfeit_timeouts를 중심으로 fixed tick state machine을 만들었다. | `make -C study/05-Game-Server-Capstone/tactical-arena-server/problem run-bot-demo` | bot demo와 state test가 같은 tick/update 규칙을 공유하게 됐다. | `cpp/src/state.cpp::step` and `cpp/src/state.cpp::build_snapshot_packet` | 실시간 서버에서 중요한 건 thread 수보다 state transition을 어디서 직렬화하느냐다. | session, room, UDP receive loop, persistence를 실제 서버 본체에 연결한다. |
| 3 | Session 3 | login/resume/list-rooms/start-match를 TCP session에서 처리하고, match result를 SQLite에 남긴다. | `cpp/src/arena_server.cpp`, `cpp/src/repository.cpp`, `docs/concepts/persistence.md`, `docs/concepts/architecture.md` | 게임 서버 설명 가능성은 결국 room lifecycle과 repository contract가 분리돼 있느냐에 달렸다. | TcpSession, RoomContext, UDP datagram dispatch, sqlite repository initialization/login/result save를 연결했다. | `make -C study/05-Game-Server-Capstone/tactical-arena-server/problem run-server` | login/create profile, room update, match result persistence가 하나의 run-server surface로 이어졌다. | `cpp/src/arena_server.cpp::handle_login` and `cpp/src/repository.cpp::login_or_create` | 네트워크 capstone은 transport보다 room/session/persistence 경계를 얼마나 분리했는지가 유지보수성을 좌우한다. | CTest, integration harness, load smoke로 전체 표면을 다시 검증한다. |
| 4 | Session 4 | unit test, integration reconnect/forfeit check, 8 bots / 2 rooms smoke를 canonical verification으로 묶는다. | `cpp/tests/test_protocol.cpp`, `cpp/tests/test_state.cpp`, `cpp/tests/test_repository.cpp`, `problem/script/integration_test.py`, `problem/script/load_smoke_test.py`, `cpp/src/arena_bot.cpp`, `cpp/src/arena_loadtest.cpp` | capstone은 단일 unit test보다 서로 다른 실행 entrypoint를 묶은 smoke contract가 필요하다. | CTest와 Python integration/load harness, bot client binaries를 canonical `make test`에 묶었다. | `make -C study/05-Game-Server-Capstone/tactical-arena-server/problem test` | CTest 3/3 PASS + integration_test ok + load_smoke_test ok | `problem/Makefile::test` | 설명 가능한 capstone은 구현만이 아니라 demo와 verification까지 source tree 안에 포함될 때 완성된다. | 남은 경계는 TLS, auth, anti-cheat, spectator, sharding 같은 production surface다. |
