# Evidence Ledger — HTTP Packet Analysis

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | Session 1 | Wireshark GUI 인상비평이 아니라 재실행 가능한 `tshark` entrypoint를 먼저 만든다. | `problem/Makefile`, `analysis/src/http-analysis.md` | analysis 프로젝트도 구현 프로젝트처럼 먼저 entrypoint를 잡지 않으면 chronology가 흐려진다. | `make -C study/03-Packet-Analysis-Top-Down/http/problem filter-basic` 같은 filter target을 기준으로 trace를 나누고, answer file의 파트 구조를 `Part 1: Basic HTTP GET / Response`, `Part 2: Conditional GET` 순으로 고정했다. | `make -C study/03-Packet-Analysis-Top-Down/http/problem filter-basic` | 어떤 질문이 어떤 trace와 filter에 기대는지 다시 찾아갈 수 있는 표면이 생겼다. | `analysis/src/http-analysis.md` | HTTP 상태 코드 해석 | answer file 안에서 파트를 실제 packet evidence로 채운다. |
| 2 | Session 2 | basic GET, conditional GET, 긴 문서 전송, embedded object 요청 체인을 packet evidence로 푸는 HTTP trace 읽기 | `analysis/src/http-analysis.md`의 `Part 1: Basic HTTP GET / Response` / `Part 2: Conditional GET` / Part 3: Long Documents and Part 4: Embedded Objects | 가장 눈에 띄는 frame만 적으면 설명이 편해 보이지만, part 순서대로 답을 채워야 문제 범위가 유지된다. | 먼저 `Part 1: Basic HTTP GET / Response`와 `Part 2: Conditional GET`를 채우고, 이어서 `Part 3: Long Documents and Part 4: Embedded Objects` 구간까지 답을 확장했다. 필요할 때는 `make -C study/03-Packet-Analysis-Top-Down/http/problem filter-conditional` 같은 filter를 다시 호출해 frame evidence를 좁혔다. | `make -C study/03-Packet-Analysis-Top-Down/http/problem filter-conditional` | analysis/src 답안이 문제의 part ordering과 같은 순서로 닫히기 시작했다. | `analysis/src/http-analysis.md` | `If-Modified-Since`와 `304 Not Modified` | docs/concepts와 verification script로 설명의 빈칸을 점검한다. |
| 3 | Session 3 | trace에서 본 사실을 개념 문서와 연결하고, answer completeness를 자동 검증한다. | `docs/concepts/conditional-get.md`, `docs/concepts/http-protocol.md`, `problem/script/verify_answers.sh` | analysis 글도 마지막에 `무엇을 봤는가`뿐 아니라 `어떤 개념으로 읽었는가`를 남겨야 다음 trace로 이어진다. | 개념 문서 제목과 trace evidence를 맞춰 보고, verify script를 다시 돌려 answer file completeness를 확인했다. | `make -C study/03-Packet-Analysis-Top-Down/http/problem test` | verify_answers.sh: `http` answer file passed content verification | `problem/script/verify_answers.sh` | HTTP 상태 코드 해석과 `If-Modified-Since`, `304 Not Modified` | 다음 분석 프로젝트에서는 다른 layer trace를 같은 방식으로 다시 읽는다. |
