# Evidence Ledger — Traceroute

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | Session 1 | TTL과 probe index마다 deterministic한 UDP destination port를 만든다. | `python/src/traceroute.py::build_probe_port`, `python/src/traceroute.py::parse_icmp_response` | traceroute에서 가장 먼저 흔들리는 부분은 어떤 ICMP reply가 어떤 probe의 것인지 매칭하는 규칙이다. | base port + ttl/probe offset 규칙을 만들고, ICMP payload 안의 embedded UDP dest port를 읽도록 파서를 작성했다. | `make -C study/04-Network-Diagnostics-and-Routing/traceroute/problem run-client HOST=8.8.8.8` | synthetic packet에서도 `(icmp_type, icmp_code, dest_port)`를 안정적으로 추출할 수 있게 됐다. | `python/src/traceroute.py::parse_icmp_response` | hop discovery는 TTL 자체보다 embedded original datagram을 다시 읽는 parsing에서 성패가 갈린다. | TTL loop와 출력 formatting을 `trace_route()`에 붙인다. |
| 2 | Session 2 | UDP probe 송신, raw ICMP 수신, 응답자 주소/RTT를 한 hop line으로 모은다. | `python/src/traceroute.py::trace_route`, `python/src/traceroute.py::format_hop_line` | 각 hop은 probe 3개를 모아서 `*`와 responder IP를 함께 보여 줘야 실제 traceroute처럼 읽힌다. | ttl 1..max_hops loop, probe별 send socket, timeout, destination 도달 시 break 조건을 구현했다. | `make -C study/04-Network-Diagnostics-and-Routing/traceroute/problem run-client HOST=8.8.8.8` | header line과 hop line 목록이 완성되면서 synthetic route에서도 1 hop씩 출력할 수 있게 됐다. | `python/src/traceroute.py::trace_route` | TTL과 `ICMP Time Exceeded`의 연결 | fake route test와 privilege 경계를 정리한다. |
| 3 | Session 3 | 실제 인터넷 경로가 없어도 TTL 증가, destination break, mixed `*` formatting을 검증한다. | `python/tests/test_traceroute.py` | live network보다 fake ICMP route map이 regression에는 더 강하다. | FakeRecvSocket, FakeSendSocket, FakeClock으로 traceroute 전체 흐름을 재현했다. | `make -C study/04-Network-Diagnostics-and-Routing/traceroute/problem test` | pytest: `../python/tests` 4 passed | `python/tests/test_traceroute.py::test_trace_route_returns_hops_until_destination` | embedded UDP port를 이용한 probe 매칭 | 같은 network layer 감각을 routing algorithm 쪽 state convergence로 옮긴다. |
