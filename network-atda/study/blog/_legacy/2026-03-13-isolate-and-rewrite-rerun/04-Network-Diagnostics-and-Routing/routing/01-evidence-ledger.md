# Evidence Ledger — Distance-Vector Routing

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | Session 1 | JSON topology를 node list와 adjacency dict로 읽고, 각 node의 초기 distance vector를 만든다. | `python/src/dv_routing.py::load_topology`, `python/src/dv_routing.py::DVNode.__init__` | Bellman-Ford 식을 적용하기 전에 self/neighbor/INF 초기값이 맞아야 이후 iteration이 설명된다. | nodes, edges를 읽어 양방향 adjacency를 만들고, self=0 / neighbor=link cost / other=INF 규칙을 고정했다. | `make -C study/04-Network-Diagnostics-and-Routing/routing/problem run-solution` | Iteration 0 출력이 topology와 같은 모양으로 정렬되기 시작했다. | `python/src/dv_routing.py::load_topology` | DV 구현은 수식보다 초기 상태를 어떻게 배치하느냐가 더 직접적으로 드러난다. | neighbor DV를 받아 Bellman-Ford relax를 수행한다. |
| 2 | Session 2 | 현재 DV와 neighbor DV를 비교해 최단 비용과 next hop을 갱신한다. | `python/src/dv_routing.py::receive_dv`, `python/src/dv_routing.py::simulate` | `D_x(y) = min_v { c(x,v) + D_v(y) }`를 iteration loop에 그대로 옮기면 convergence 로그를 만들 수 있다. | message snapshot 수집, neighbor delivery, any_changed 검사, convergence break 조건을 구현했다. | `make -C study/04-Network-Diagnostics-and-Routing/routing/problem run-solution` | Iteration N 출력과 final routing table이 topology별 shortest path와 맞아떨어졌다. | `python/src/dv_routing.py::receive_dv` | Bellman-Ford update 식 | topology fixture로 convergence를 자동 검증한다. |
| 3 | Session 3 | 3-node, 5-node topology에서 shortest path와 next hop이 맞는지 회귀 검증한다. | `python/tests/test_dv_routing.py`, `docs/concepts/bellman-ford.md`, `docs/concepts/count-to-infinity.md` | DV는 수렴 결과만 아니라 count-to-infinity 같은 한계까지 같이 적어야 이해가 닫힌다. | fixture topology를 만들고 loader/node 동작을 test로 잠근 뒤, 한계와 비교 문서를 보강했다. | `make -C study/04-Network-Diagnostics-and-Routing/routing/problem test` | Distance-Vector Routing Test Suite: 3-node/5-node topology convergence 모두 PASS | `python/tests/test_dv_routing.py::TestDVNode` | 2-phase synchronous simulation | 다음 단계에서는 알고리즘 과제를 넘어 game server capstone으로 여러 layer를 통합한다. |
