# Evidence Ledger — ICMP Pinger

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | Session 1 | raw socket을 열기 전에 packet bytes가 스스로 성립하는지 먼저 확인한다. | `python/src/icmp_pinger.py::internet_checksum`, `python/src/icmp_pinger.py::build_echo_request` | raw socket 문제의 첫 실패 지점은 OS 권한보다 잘못된 checksum일 가능성이 높다. | odd-length padding, carry folding, timestamp payload, checksum 재삽입 순서를 코드로 고정했다. | `make -C study/04-Network-Diagnostics-and-Routing/icmp-pinger/problem test` | packet 전체로 checksum을 다시 계산했을 때 0이 되는 테스트가 생겼다. | `python/src/icmp_pinger.py::internet_checksum` | ICMP Echo는 header보다 payload에 timestamp를 어떻게 넣고 되읽느냐가 RTT 측정의 중심이다. | 수신 packet에서 IHL을 읽어 reply를 파싱한다. |
| 2 | Session 2 | IHL 기반 ICMP offset 계산과 select timeout을 실제 ping loop에 연결한다. | `python/src/icmp_pinger.py::parse_echo_reply`, `python/src/icmp_pinger.py::ping` | IPv4 header 길이를 건너뛰지 않으면 sequence, identifier, payload timestamp를 믿을 수 없다. | IP header length 계산, identifier match, timeout handling, 1초 주기 유지까지 ping loop 안에 넣었다. | `sudo make -C study/04-Network-Diagnostics-and-Routing/icmp-pinger/problem test-live HOST=google.com` | 성공 reply, timeout, loss statistics가 같은 출력 표면에 정리됐다. | `python/src/icmp_pinger.py::parse_echo_reply` | RFC 1071 인터넷 체크섬 | 실제 raw socket 대신 fake socket으로 deterministic test를 고정한다. |
| 3 | Session 3 | 권한 없는 환경에서도 checksum, packet build, RTT 출력, loss 통계를 재현 가능하게 만든다. | `python/tests/test_icmp_pinger.py`, `problem/script/test_icmp.sh` | live raw-socket 검증은 있어도, 회귀는 fake socket 기반 deterministic test가 담당해야 한다. | FakeRawSocket, FakeClock, monkeypatch 기반 test와 별도 live script를 나눴다. | `make -C study/04-Network-Diagnostics-and-Routing/icmp-pinger/problem test` | pytest: `../python/tests` 11 passed; live raw-socket check는 sudo 별도 | `python/tests/test_icmp_pinger.py::FakeRawSocket` | raw socket 권한 모델 | 비슷한 raw ICMP surface를 traceroute hop correlation 쪽으로 확장한다. |
