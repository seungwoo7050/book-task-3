# 03 Index Filter — Evidence Ledger

기존 blog 본문은 입력 근거에서 제외했다. 이번 재작성은 `README`, `problem/README.md`, `docs/concepts/*`, `src/index_filter/table.py`, `tests/test_index_filter.py`, 실제 재실행 CLI만으로 다시 세웠다.

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Phase 1 | 이 슬롯의 범위를 "Bloom filter 구현"에서 읽기 비용 최적화로 다시 잡는다 | `README.md`, `problem/README.md`, `tests/test_index_filter.py` | false positive rate 설명이 중심일 거라고 봤다. | 테스트를 다시 읽어 bloom reject와 bounded scan이 둘 다 공개 계약이라는 점을 먼저 고정했다. | `PYTHONPATH=src python3 -m pytest` | `test_sstable_bloom_reject_and_bounded_scan`가 `bytes_read == 0` miss와 `0 < bytes_read < data_size` hit를 함께 요구했다. | `test_sstable_bloom_reject_and_bounded_scan` | 이 슬롯의 주제는 정확도 자체보다 lookup cost 경계다. | 이제 filter, index, footer가 실제로 어떤 파이프를 만드는지 코드를 다시 본다. |
| 2 | Phase 2 | bloom reject와 bounded block scan이 어디서 잠기는지 확인한다 | `src/index_filter/table.py` | Bloom filter만 이해하면 거의 끝날 것 같았다. | `BloomFilter`, `SparseIndex`, `SSTable.write/load/get_with_stats()`를 다시 읽어 data/filter/index/footer 배치와 stats 경로를 묶었다. | `PYTHONPATH=src python3 - <<'PY' ... PY` | `footer_magic b'SIF1'`, `missing ... bytes_read=0`, `hit ... bytes_read=128 block_range=(256, 384)`가 실제 file layout과 비용 절감을 보여 줬다. | `get_with_stats()` | filter와 index는 경쟁 관계가 아니라 miss/hit를 나눠 맡는 두 단계 파이프다. | pytest와 demo를 다시 돌려 숫자와 현재 한계를 정리한다. |
| 3 | Phase 3 | demo 표면과 source-based limitation을 함께 정리한다 | `src/index_filter/__main__.py`, `docs/concepts/bloom-filter-sizing.md` | 문서 설명과 소스 해시 구현이 같을 거라고 생각했다. | demo와 개념 문서를 source와 다시 대조해, bounded scan demo는 유지하되 해시 구현과 placeholder 반환값 차이도 같이 남겼다. | `PYTHONPATH=src python3 -m pytest`<br>`PYTHONPATH=src python3 -m index_filter` | demo는 `{'found': True, 'value': 'value-k023', 'bytes_read': 176}`를 출력했고, source는 MurmurHash3가 아니라 SHA-256 기반 double hashing을 쓴다. | `_hash_value()`, `demo()` | docs보다 source가 진짜 contract다. | 다음 프로젝트는 disk-backed page cache로 읽기 비용을 다른 층위에서 다룬다. |
