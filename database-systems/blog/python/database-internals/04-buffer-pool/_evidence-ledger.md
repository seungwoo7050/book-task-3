# 04 Buffer Pool — Evidence Ledger

기존 blog 본문은 입력 근거에서 제외했다. 이번 재작성은 `README`, `problem/README.md`, `docs/concepts/*`, `src/buffer_pool/core.py`, `tests/test_buffer_pool.py`, 실제 재실행 CLI만으로 다시 세웠다.

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Phase 1 | 이 슬롯의 범위를 cache 구현에서 page lifecycle 관리로 다시 잡는다 | `README.md`, `problem/README.md`, `tests/test_buffer_pool.py` | hit ratio나 LRU 자체가 중심일 거라고 봤다. | 테스트를 다시 읽어 LRU layer와 BufferPool layer를 분리하고, pin/dirty/eviction 경계가 핵심이라는 점을 먼저 고정했다. | `PYTHONPATH=src python3 -m pytest` | `7 passed`보다 `test_track_dirty_pages`, `test_eviction_after_unpin`가 page lifecycle contract를 잠근다는 점이 중요했다. | `test_eviction_after_unpin` | buffer pool은 cache API보다 residency semantics가 더 중요하다. | 이제 source에서 LRU, pin, dirty가 실제로 어떻게 맞물리는지 본다. |
| 2 | Phase 2 | fetch/unpin/flush/evict invariant가 코드에서 어디서 잠기는지 확인한다 | `src/buffer_pool/core.py` | replacement policy만 이해하면 대부분 설명될 것 같았다. | `LRUCache`, `BufferPool.fetch_page`, `unpin_page`, `flush_page`를 다시 읽어 MRU ordering, pin acquisition, dirty write-back, file-backed read path를 묶었다. | `PYTHONPATH=src python3 -m buffer_pool` | demo는 `pin_count: 1`, `prefix: page-0`를 보여 줬다. | `fetch_page()`, `unpin_page()` | fetch는 read와 동시에 pin acquisition이기도 하다. | pinned eviction 실패 경로가 실제로 얼마나 안전한지 보조 재실행으로 확인한다. |
| 3 | Phase 3 | 실패 경로와 현재 seam을 문서화한다 | `src/buffer_pool/core.py`, 보조 재실행 | pinned eviction은 오류만 내고 상태는 그대로일 거라고 생각했다. | LRU가 pinned page를 희생하려는 상황을 직접 만들어 보고, error 뒤 cache state를 확인했다. | `PYTHONPATH=src python3 - <<'PY' ... PY` | `RuntimeError` 뒤에도 cache keys가 `['...:0', '...:2']`로 바뀌어 완전 rollback이 아니었다. | `fetch_page()`의 evict-then-reinsert 흐름 | failed eviction도 state semantics의 일부다. | 다음 프로젝트는 MVCC에서 cache가 아니라 visibility와 version chain으로 관심을 옮긴다. |
