# 02 WAL Recovery — Evidence Ledger

기존 blog 본문은 입력 근거에서 제외했다. 이번 재작성은 `README`, `problem/README.md`, `docs/concepts/*`, `src/wal_recovery/store.py`, `tests/test_wal_recovery.py`, 실제 재실행 CLI만으로 다시 세웠다.

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Phase 1 | 이 슬롯의 범위를 "로그 파일 추가"에서 durable write path로 다시 잡는다 | `README.md`, `problem/README.md`, `tests/test_wal_recovery.py` | WAL parser 정도가 중심일 거라고 봤다. | 테스트 이름과 문제 bullet을 다시 읽어 replay, corruption, reopen, WAL rotation까지 공개 범위라는 점을 고정했다. | `PYTHONPATH=src python3 -m pytest` | `7 passed` 자체보다 `test_stop_at_corrupted_record`, `test_force_flush_rotates_wal`가 recovery policy와 file lifecycle을 함께 잠근다는 점이 중요했다. | `test_force_flush_rotates_wal` | WAL은 로깅 기능이 아니라 write acknowledgement 경계를 바꾼다. | 이제 코드에서 append-before-apply와 stop-on-corruption이 실제로 어디서 고정되는지 본다. |
| 2 | Phase 2 | append/recover/rotate invariant를 코드에서 확인한다 | `src/wal_recovery/store.py` | CRC만 이해하면 충분할 것 같았다. | `_append_record`, `recover`, `DurableStore.open`, `force_flush`를 다시 읽어 WAL record format, prefix recovery, WAL rotation, memtable replay를 묶었다. | `PYTHONPATH=src python3 - <<'PY' ... PY` | `before_flush_wal_size 37`, `after_flush_wal_size 0`, `recovered_after_reopen [('put', 'gamma', '3')]`, `corrupt_replay [...]`가 실제 파일 상태를 보여 줬다. | `_append_record`, `recover`, `force_flush` | flush는 SSTable 생성과 WAL lifecycle cut-over를 동시에 수행한다. | 테스트와 demo를 다시 돌려 공개 표면과 남는 한계를 정리한다. |
| 3 | Phase 3 | recovery의 공개 표면과 비워 둔 durability 범위를 정리한다 | `tests/test_wal_recovery.py`, `src/wal_recovery/__main__.py` | demo가 flush/recovery 전체를 보여 줄 거라고 생각했다. | pytest와 demo를 다시 돌려, demo는 최소 recovery 표면만 보여 주고 세밀한 file policy는 테스트와 보조 재실행이 맡는 구조로 정리했다. | `PYTHONPATH=src python3 -m pytest`<br>`PYTHONPATH=src python3 -m wal_recovery` | demo는 `{'recovered': True, 'value': '1'}` 한 줄로 recovery 존재만 보여 주고, 테스트가 corruption/rotation 같은 세부 정책을 잠근다. | `demo()` | `fsync_enabled`는 아직 `flush()`만 호출할 뿐 진짜 fsync barrier는 아니다. | 다음 프로젝트는 lookup 비용을 줄이기 위해 index/filter를 붙인다. |
