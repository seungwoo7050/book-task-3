# 05 MVCC — Evidence Ledger

기존 blog 본문은 입력 근거에서 제외했다. 이번 재작성은 `README`, `problem/README.md`, `docs/concepts/*`, `src/mvcc_lab/core.py`, `tests/test_mvcc.py`, 실제 재실행 CLI만으로 다시 세웠다.

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Phase 1 | 이 슬롯의 범위를 transaction generality가 아니라 snapshot visibility core로 다시 잡는다 | `README.md`, `problem/README.md`, `tests/test_mvcc.py` | MVCC라서 transaction 기능 전반을 다루는 줄 알았다. | 테스트와 문제 정의를 다시 읽어 read-your-own-write, snapshot, conflict, cleanup, gc만 deliberate scope라는 점을 먼저 고정했다. | `PYTHONPATH=src python3 -m pytest` | `7 passed`보다 `test_snapshot_isolation`, `test_write_write_conflict`, `test_gc`가 visibility contract를 분명히 잠근다는 점이 중요했다. | `test_snapshot_isolation` | 이 슬롯의 중심은 저장이 아니라 visibility rule이다. | 이제 version chain과 transaction manager가 그 rule을 어디서 고정하는지 코드를 다시 본다. |
| 2 | Phase 2 | snapshot watermark, conflict cleanup, GC가 실제로 어디서 잠기는지 확인한다 | `src/mvcc_lab/core.py` | version chain만 보면 대부분 설명될 것 같았다. | `begin`, `read`, `commit`, `_abort_internal`, `gc`를 다시 읽어 snapshot watermark, read-your-own-write, first-committer-wins, cleanup, chain pruning을 묶었다. | `PYTHONPATH=src python3 - <<'PY' ... PY` | `snapshot_read v1`, conflict 후 `chain_after_conflict [(4, 'a', False)]`, GC 후 `[(3, 'v3', False)]`가 실제 chain semantics를 보여 줬다. | `begin()`, `commit()`, `gc()` | conflict 처리와 GC는 visibility rule의 일부이지 부수 작업이 아니다. | 테스트와 demo를 다시 돌려 공개 표면과 현재 한계를 정리한다. |
| 3 | Phase 3 | public demo와 aggressive GC semantics를 함께 정리한다 | `src/mvcc_lab/__main__.py`, `tests/test_mvcc.py` | demo가 snapshot과 conflict까지 보여 줄 거라고 생각했다. | demo는 read-your-own-write 최소 표면만 노출하고, deeper semantics는 테스트와 보조 재실행이 맡는 구조로 정리했다. | `PYTHONPATH=src python3 -m pytest`<br>`PYTHONPATH=src python3 -m mvcc_lab` | demo는 `{'tx': 1, 'read_your_own_write': 10}`만 출력하고, GC는 active reader가 없으면 chain을 최신 version 하나로까지 줄일 수 있다. | `demo()` | docs보다 runtime chain 모양이 현재 semantics를 더 정확히 보여 준다. | 다음 프로젝트부터는 분산 경로로 넘어가 visibility 대신 RPC/replication surface를 다룬다. |
