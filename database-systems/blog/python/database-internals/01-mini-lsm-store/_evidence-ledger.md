# 01 Mini LSM Store — Evidence Ledger

기존 blog 본문은 입력 근거에서 제외했다. 이번 재작성은 `README`, `problem/README.md`, `docs/concepts/*`, `src/mini_lsm_store/store.py`, `tests/test_mini_lsm_store.py`, 실제 재실행 CLI만으로 다시 세웠다.

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Phase 1 | 이 슬롯의 실제 범위를 다시 좁힌다 | `README.md`, `problem/README.md`, `tests/test_mini_lsm_store.py` | 첫 프로젝트라 CRUD 수준에서 멈출 거라고 봤다. | 테스트 이름과 문제 bullet을 먼저 읽어 flush, tombstone, reopen이 이미 범위 안이라는 점을 고정했다. | `PYTHONPATH=src python3 -m pytest` | `9 passed` 자체보다 `test_tombstone_across_levels`, `test_persistence_after_reopen`가 공개 계약에 들어 있다는 점이 중요했다. | `test_persistence_after_reopen` | 첫 슬롯인데도 read visibility와 persistence 경계를 함께 잡는다. | 이제 실제 코드를 읽어 그 계약이 어디서 잠기는지 본다. |
| 2 | Phase 2 | flush/read/reopen invariant가 실제로 어디서 고정되는지 확인한다 | `src/mini_lsm_store/store.py` | `SSTable`만 보면 충분할 것 같았다. | `force_flush`, `get`, `open`, `_replace_memtable_value`를 다시 읽어 active/immutable, tombstone `None`, newest-first ordering, byte threshold를 묶었다. | `PYTHONPATH=src python3 - <<'PY' ... PY` | 보조 재실행에서 reopen 뒤 파일 순서가 `['000002.sst', '000001.sst']`, tombstone key가 `(None, True)`로 유지됐다. | `force_flush`, `get`, `open` | tombstone은 "없음"이 아니라 "삭제 기록 존재"라는 의미다. | 테스트와 demo를 다시 돌려 공개 표면과 한계를 정리한다. |
| 3 | Phase 3 | 현재 공개 표면과 다음 슬롯으로 넘기는 경계를 정리한다 | `tests/test_mini_lsm_store.py`, `src/mini_lsm_store/__main__.py` | demo는 테스트의 축약판 정도일 거라고 생각했다. | pytest와 demo를 다시 돌려, 테스트가 지키는 넓은 계약과 demo가 보여 주는 최소 메시지를 분리해 정리했다. | `PYTHONPATH=src python3 -m pytest`<br>`PYTHONPATH=src python3 -m mini_lsm_store` | pytest는 9개 invariant를 잡고, demo는 `{'key': 'alpha', 'found': True, 'value': '3', 'sstables': 1}`로 newest-first visibility만 노출했다. | `demo()` | WAL, compaction, filters가 왜 다음 슬롯에서 등장하는지가 이 경계에서 선명해진다. | 다음 프로젝트는 durable write path를 위해 WAL을 붙인다. |
