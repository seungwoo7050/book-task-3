# 01 RPC Framing — Evidence Ledger

기존 blog 본문은 입력 근거에서 제외했다. 이번 재작성은 `README`, `problem/README.md`, `docs/concepts/*`, `src/rpc_framing/core.py`, `tests/test_rpc_framing.py`, 실제 재실행 CLI만으로 다시 세웠다.

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Phase 1 | 이 슬롯의 범위를 RPC 기능 구현에서 wire contract 고정으로 다시 잡는다 | `README.md`, `problem/README.md`, `tests/test_rpc_framing.py` | handler registration이나 client API가 중심일 거라고 봤다. | 테스트와 문제 정의를 다시 읽어 boundary recovery와 pending map이 먼저라는 점을 고정했다. | `PYTHONPATH=src python3 -m pytest` | `5 passed`보다 `test_decoder_handles_split_chunks`, `test_rpc_handles_concurrent_calls`가 이 슬롯의 실제 중심을 보여 줬다. | `test_rpc_handles_concurrent_calls` | RPC의 핵심은 method보다 wire contract다. | 이제 decoder와 pending map이 실제로 어디서 그 계약을 만드는지 코드를 다시 본다. |
| 2 | Phase 2 | split chunk, pending correlation, error propagation이 어디서 잠기는지 확인한다 | `src/rpc_framing/core.py` | framing만 이해하면 거의 끝날 것 같았다. | `FrameDecoder`, `RPCClient.call/_read_loop/_fail_all`, `RPCServer._dispatch`를 다시 읽어 boundary recovery, correlation routing, timeout/error fan-out을 묶었다. | `PYTHONPATH=src python3 - <<'PY' ... PY` | `split1 []`, `split2 []`, `split3 [...]`, `unknown RuntimeError ...`, `timeout TimeoutError ...`가 실제 contract를 보여 줬다. | `FrameDecoder.feed()`, `RPCClient.call()` | client의 핵심은 socket보다 pending map이다. | demo와 current seams를 정리해 public surface와 test gap을 분리한다. |
| 3 | Phase 3 | public demo와 source-based seam을 함께 정리한다 | `src/rpc_framing/__main__.py`, `_fail_all()` 경로 | disconnect failure fan-out도 테스트가 직접 덮고 있을 줄 알았다. | demo는 happy path를 다시 확인하고, source를 읽어 disconnect path는 구현돼 있지만 별도 테스트로 고정되진 않는다는 점을 함께 남겼다. | `PYTHONPATH=src python3 -m pytest`<br>`PYTHONPATH=src python3 -m rpc_framing` | demo는 `{'msg': 'hello'}` 한 줄로 wire round trip을 보여 주고, disconnect fan-out은 source-based seam으로 남았다. | `_fail_all()`, `demo()` | test coverage와 source semantics는 분리해서 읽어야 한다. | 다음 프로젝트는 replication 경로로 넘어가 같은 RPC surface 위에 leader/follower 흐름을 올린다. |
