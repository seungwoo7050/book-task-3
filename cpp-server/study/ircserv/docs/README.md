# ircserv Docs

## Key Concepts

- capstone은 새 기능 추가보다 앞선 lab 경계를 다시 합치는 데 의미가 있다.
- `MODE`와 `INVITE`는 channel state와 privilege model을 동시에 건드린다.
- `CAP`는 최소 지원만 하더라도 client compatibility를 높여 준다.

## Reference Pointers

- `legacy/README.md`
- `legacy/src/Executor.cpp`
- `legacy/src/Server.cpp`
