# Client Boundary

`incident-ops-mobile`과 `incident-ops-mobile-client`는 의도적으로 분리돼 있다.

- `incident-ops-mobile`: canonical contract + backend + harness
- `incident-ops-mobile-client`: portfolio-grade product client

이 분리는 시스템 학습과 제품 완성도를 섞지 않게 해 준다.
