# React Native Implementation

Status: verified

이 앱은 Battery, Haptics, Sensor spec을 읽어 consumer card와 generated summary를 보여 주는 구현이다.

## Commands

```bash
npm install
npm run codegen
npm run typecheck
npm test
npm run verify
```
