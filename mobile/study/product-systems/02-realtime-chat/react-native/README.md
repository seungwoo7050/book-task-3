# React Native Implementation

Status: verified

이 앱은 local-first message model, replay, ack reconcile, typing/presence 규칙을 보여 주는 구현이다.

## Commands

```bash
npm install
npm run typecheck
npm test
npm run verify
```
