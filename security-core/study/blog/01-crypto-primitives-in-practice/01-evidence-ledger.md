# Evidence Ledger — crypto-primitives-in-practice

이 표는 최종 글보다 앞에서 “무엇을 어떤 순서로 만들었는지”를 사실 중심으로 다시 세운 메모다. 문장보다 근거를 먼저 확인하고 싶을 때 읽으면 된다.

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Session 1 | hash, MAC, KDF를 같은 함수처럼 보이지 않게 primitive surface부터 나눈다. | `python/src/crypto_primitives_in_practice/primitives.py::sha256_digest`, `hmac_sha256`, `hkdf_sha256`, `pbkdf2_hmac_sha1`, `constant_time_compare` | 함수 경계가 먼저 갈라져야 vector와 demo도 흔들리지 않는다. | SHA-256, HMAC-SHA256, HKDF-SHA256, PBKDF2-HMAC-SHA1을 독립 함수로 두고 HKDF length, PBKDF2 iteration guard를 넣었다. | `PYTHONPATH=study/01-crypto-primitives-in-practice/python/src .venv/bin/python -m pytest study/01-crypto-primitives-in-practice/python/tests` | `10 passed in 0.06s`로 primitive와 CLI 계층이 함께 닫혔다. | `hkdf_sha256()`의 extract-expand loop와 `constant_time_compare()` | plain hash와 keyed MAC, password KDF와 key expansion KDF는 입력의 성격이 다르다. | 계산 함수를 manifest와 demo JSON에 연결한다. |
| 2 | Session 2 | primitive를 설명문이 아니라 reference vector 판정기로 고정한다. | `python/src/crypto_primitives_in_practice/vectors.py::_decode`, `_compute_actual_hex`, `check_vectors_manifest` | fixture를 읽는 층이 없으면 primitive 분리가 검증 근거로 이어지지 않는다. | encoding 선택, primitive dispatch, `actual_hex`와 `expected_hex` 비교를 한 manifest summary로 묶었다. | `PYTHONPATH=study/01-crypto-primitives-in-practice/python/src .venv/bin/python -m crypto_primitives_in_practice.cli check-vectors study/01-crypto-primitives-in-practice/problem/data/hkdf_sha256_vectors.json` | `manifest=hkdf_sha256_vectors.json`, `passed=1`, `failed=0`, `rfc-5869-case-1 matched=true` | `_compute_actual_hex()`와 `check_vectors_manifest()` | reference vector는 구현 검증일 뿐 아니라 primitive가 무엇을 계산하는지 고정하는 장치다. | 사람이 읽는 demo output으로 primitive 차이를 한 번 더 보여 준다. |
| 3 | Session 3 | vector 통과에서 멈추지 않고 demo와 CLI 계약까지 닫는다. | `python/src/crypto_primitives_in_practice/vectors.py::demo_from_profile`, `python/src/crypto_primitives_in_practice/cli.py`, `python/tests/test_cli.py` | pass/fail 수치만 남기면 왜 hash와 MAC을 갈랐는지 다시 흐려진다. | demo profile에서 hash, mac, kdf 결과와 비교 포인트를 함께 출력하고 CLI 테스트로 JSON shape를 고정했다. | `PYTHONPATH=study/01-crypto-primitives-in-practice/python/src .venv/bin/python -m crypto_primitives_in_practice.cli demo study/01-crypto-primitives-in-practice/problem/data/demo_profile.json` | `hash_equals_mac=false`, `mac_self_check=true`, HKDF base64url까지 deterministic하게 출력됐다. | `demo_from_profile()`의 `comparisons` 블록 | “같은 입력이면 같은 출력”이라는 hash 성질은 인증보다 식별에 가깝다는 점이 demo에서 더 선명해진다. | 최종 글에서는 primitive surface -> vector contract -> demo contract 순서로 복원한다. |
