# 01 Semantic Layouts And A11y evidence ledger

`46051f3` 한 번에 코드와 테스트가 크게 들어왔기 때문에, 세부 chronology는 README, `vanilla/src/*`, 테스트, 재검증 CLI를 기준으로 다시 복원했다.

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-03-08 / Phase 1 | 프로젝트 경계와 public surface를 먼저 고정한다 | `README.md`, `problem/README.md`, `vanilla/README.md`, `package.json` | 설정 화면이면 폼만 만들면 된다고 보기 쉽지만, 이 프로젝트의 답은 semantic shell 자체일 거라고 봤다. | README의 문제 질문과 verify 명령을 기준으로 landmark, label, keyboard flow를 먼저 계약으로 묶었다. | `git log --reverse --stat -- study/frontend-foundations/01-semantic-layouts-and-a11y | sed -n '1,40p'` | 2026-03-08 `A large commit`에 `README`, `problem`, `vanilla`, `tests`, `vite/vitest/playwright`가 한꺼번에 들어왔다. | `vanilla/src/app.ts` / `getAppMarkup` | 접근성은 나중 polish가 아니라 첫 마크업 구조에서 시작한다. | validation과 focus 이동을 실제 DOM 상태 전이로 좁힌다. |
| 2 | 2026-03-08 / Phase 2 | semantic 구조와 validation을 같은 루프에서 연결한다 | `vanilla/src/app.ts`, `vanilla/src/validation.ts` | 오류 메시지는 submit 시점에만 붙이면 충분할 줄 알았지만, blur와 focus 복원까지 묶여야 keyboard flow가 닫힌다고 봤다. | `updateErrorState`, `focusFirstInvalidField`, `mountSettingsShell`을 중심으로 `aria-invalid`, inline error, submit status를 한 루프로 묶었다. | `rg -n 'updateErrorState|mountSettingsShell|validateSettings|focusFirstInvalidField' study/frontend-foundations/01-semantic-layouts-and-a11y/vanilla/src/app.ts study/frontend-foundations/01-semantic-layouts-and-a11y/vanilla/src/validation.ts` | `updateErrorState`, `focusFirstInvalidField`, `mountSettingsShell`, `validateSettings` 네 지점이 validation과 focus 흐름의 중심축으로 드러났다. | `vanilla/src/app.ts` / `updateErrorState`, `mountSettingsShell` | `help/error text pairing`은 텍스트 배치가 아니라 `aria-invalid`와 focus 순서를 함께 설계해야 성립한다. | 구조 검증과 keyboard smoke로 contract를 닫는다. |
| 3 | 2026-03-12 / Phase 3 | 공개 문서와 한계를 코드 범위에 맞춰 다시 정리한다 | `README.md`, `problem/README.md`, `docs/README.md`, `vanilla/README.md` | 구현이 있어도 README가 기능 목록으로 흘러가면 이후 프로젝트와의 연결이 약해진다고 봤다. | 2026-03-12 문서 polish에서 포함/제외 범위, verify surface, 다음 단계 연결을 다시 고정했다. | `git log --reverse --stat -- study/frontend-foundations/01-semantic-layouts-and-a11y | sed -n '41,80p'` | `0e12fb8`가 README/problem/docs만 건드렸고, 이는 코드보다 public contract를 다듬는 단계였다는 신호다. | `README.md` / 검증 섹션 | code-first blog에서도 README는 "무엇을 검증했는가"를 닫아 주는 계약 문서다. | 실제 verify를 다시 실행해 현재 상태를 확인한다. |
| 4 | 2026-03-13 / Reverify | canonical verify를 다시 실행해 source-first 글의 마지막 근거를 확보한다 | `vanilla/tests/validation.test.ts`, `vanilla/tests/shell.test.ts`, `vanilla/tests/semantic-layout.spec.ts` | unit test만 통과해도 충분할 수 있지만, 이 프로젝트는 keyboard 흐름까지 보여 줘야 한다고 봤다. | `npm run verify --workspace @front-react/semantic-layouts-a11y`를 다시 실행해 vitest와 Playwright 둘 다 통과시키고 결과를 고정했다. | `cd study && npm run verify --workspace @front-react/semantic-layouts-a11y` | `5 passed` unit tests, Playwright `2 passed (7.3s)` | `vanilla/tests/semantic-layout.spec.ts` / landmark + keyboard submission | 접근성 프로젝트는 static markup 설명만으로 끝나지 않고 실제 tab/submit 흐름까지 닫혀야 한다. | local persistence와 routing이 없는 현재 경계를 마지막에 명시한다. |
