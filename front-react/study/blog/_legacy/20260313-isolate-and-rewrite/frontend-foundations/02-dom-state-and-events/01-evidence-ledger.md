# 02 DOM State And Events evidence ledger

이 프로젝트도 핵심 구현이 2026-03-08의 큰 commit 하나에 모여 있다. 그래서 chronology는 `README -> state helper -> board event loop -> verify` 순서로 재구성했다.

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-03-08 / Phase 1 | 이 프로젝트의 상태 경계를 먼저 고정한다 | `README.md`, `problem/README.md`, `vanilla/README.md`, `package.json` | selection과 inline edit가 보여서 UI interaction만 중심일 줄 알았지만, README를 보니 URL query와 persistence가 더 핵심 축이었다. | README의 문제 질문과 verify surface를 기준으로 URL state, local state, persistence를 먼저 contract로 묶었다. | `git log --reverse --stat -- study/frontend-foundations/02-dom-state-and-events | sed -n '1,40p'` | 2026-03-08 commit에 `state.ts`, `app.ts`, tests, `playwright.config.ts`가 같이 들어왔다. | `vanilla/src/state.ts` / `createInitialBoardState` | DOM 이벤트 설계는 state boundary를 정한 다음에야 읽힌다. | query serialization과 persistence helper를 먼저 읽는다. |
| 2 | 2026-03-08 / Phase 2 | URL query와 local persistence를 한 state 모델로 묶는다 | `vanilla/src/state.ts` | filter와 sort 정도만 URL에 올리면 될 줄 알았지만, selection 복원까지 state reconciliation이 필요했다. | `parseQuery`, `serializeQuery`, `savePersistedState`, `reconcileSelection`, `createInitialBoardState`를 중심으로 state 모델을 좁혔다. | `rg -n 'serializeQuery|createInitialBoardState|savePersistedState|reconcileSelection' study/frontend-foundations/02-dom-state-and-events/vanilla/src/state.ts` | `serializeQuery`, `savePersistedState`, `reconcileSelection`, `createInitialBoardState`가 같은 파일에 모여 있어 URL/local state 경계가 명확했다. | `vanilla/src/state.ts` / `serializeQuery`, `reconcileSelection` | query state와 local UI state는 저장 위치만 다른 게 아니라 복원 규칙도 다르다. | event delegation이 이 state를 어떻게 다시 그리는지 본다. |
| 3 | 2026-03-08 / Phase 3 | rerender 이후에도 keyboard 흐름이 유지되는 event loop를 만든다 | `vanilla/src/app.ts`, `vanilla/tests/board.spec.ts` | 매 이벤트마다 DOM을 다시 그리면 focus가 깨질 거라고 봤다. | `mountBoard` 안에서 `render`, `setQuery`, `setSelection`, `setEditing`을 만들고, `input`/`change`/`click`/`keydown`를 root delegation으로 붙였다. | `rg -n 'mountBoard|createInitialBoardState|savePersistedState|serializeQuery' study/frontend-foundations/02-dom-state-and-events/vanilla/src/app.ts study/frontend-foundations/02-dom-state-and-events/vanilla/src/state.ts` | `mountBoard`가 `reconcileSelection -> innerHTML -> syncUrl -> savePersistedState`를 한 render 루프로 묶고 있었다. | `vanilla/src/app.ts` / `render`, `setEditing` | rerender 기반 UI라도 focus 복원 지점을 설계하면 keyboard 흐름을 유지할 수 있다. | verify로 query -> select -> edit -> save 흐름을 닫는다. |
| 4 | 2026-03-13 / Reverify | canonical verify를 다시 실행해 state/event contract를 현재 시점에 닫는다 | `vanilla/tests/state.test.ts`, `vanilla/tests/shell.test.ts`, `vanilla/tests/board.spec.ts` | unit test만 보면 충분해 보이지만, URL sync와 inline edit는 E2E까지 봐야 한다고 판단했다. | `npm run verify --workspace @front-react/dom-state-and-events`를 다시 실행해 vitest와 Playwright 양쪽을 확인했다. | `cd study && npm run verify --workspace @front-react/dom-state-and-events` | unit `6 passed`, Playwright `2 passed (3.1s)` | `vanilla/tests/board.spec.ts` / query -> select -> edit 시나리오 | state 동기화는 helper correctness와 interaction continuity를 같이 확인해야 한다. | 비동기 요청 상태가 없는 현재 한계를 마지막에 분리한다. |
