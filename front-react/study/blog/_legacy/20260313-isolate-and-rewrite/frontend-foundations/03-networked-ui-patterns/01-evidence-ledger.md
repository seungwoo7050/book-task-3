# 03 Networked UI Patterns evidence ledger

이 프로젝트의 chronology는 one-shot commit 안에서 비동기 상태 전이가 어떻게 나뉘는지 다시 읽는 작업에 가깝다. 특히 `service.ts`, `state.ts`, `app.ts`의 분리가 판단 전환점을 잘 보여 준다.

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-03-08 / Phase 1 | 비동기 UI 범위를 public contract로 먼저 고정한다 | `README.md`, `problem/README.md`, `vanilla/README.md`, `package.json` | mock API 프로젝트라서 service helper가 중심일 줄 알았지만, README를 보니 request lifecycle과 URL navigation을 함께 설명해야 했다. | loading/empty/error/retry/abort/stale response가 전부 포함 범위라는 점과 `npm run verify --workspace @front-react/networked-ui-patterns`를 canonical CLI로 고정했다. | `git log --reverse --stat -- study/frontend-foundations/03-networked-ui-patterns | sed -n '1,40p'` | 2026-03-08 large commit에 `service.ts`, `state.ts`, `app.ts`, explorer tests, Playwright config가 함께 들어왔다. | `README.md` / 검증 섹션 | 비동기 UI는 fetch 함수 하나보다 lifecycle contract가 먼저다. | URL state와 request tracker를 먼저 읽는다. |
| 2 | 2026-03-08 / Phase 2 | stale response를 막는 최소 invariant를 만든다 | `vanilla/src/state.ts`, `vanilla/src/app.ts` | abort만 넣으면 충분할 줄 알았지만, abort가 늦었을 때를 막으려면 token 기반 latest check도 필요하다고 봤다. | `parseUrlState`, `serializeUrlState`, `createRequestTracker`, `loadDetail`, `loadList`를 중심으로 navigation state와 request ordering을 묶었다. | `rg -n 'createRequestTracker|parseUrlState|serializeUrlState|loadDetail|loadList' study/frontend-foundations/03-networked-ui-patterns/vanilla/src/state.ts study/frontend-foundations/03-networked-ui-patterns/vanilla/src/app.ts` | `createRequestTracker`, `loadDetail`, `loadList`가 stale response 무시의 핵심 anchor로 잡혔다. | `vanilla/src/state.ts` / `createRequestTracker`, `vanilla/src/app.ts` / `loadList` | 비동기 UI에서 중요한 건 request를 보낸 사실이 아니라, 어떤 응답만 state를 바꿀 자격이 있는가다. | mock service와 verify 시나리오로 contract를 닫는다. |
| 3 | 2026-03-12 / Phase 3 | 공개 문서를 실제 request lifecycle 설명에 맞춘다 | `README.md`, `problem/README.md`, `docs/README.md` | 구현만 남겨 두면 race/abort 설계가 README 요약에서 사라질 수 있다고 봤다. | 2026-03-12 문서 polish에서 lifecycle, query navigation, limitation을 공개 surface로 다시 정리했다. | `git log --reverse --stat -- study/frontend-foundations/03-networked-ui-patterns | sed -n '41,80p'` | `0e12fb8`는 코드보다 README/problem/docs를 다듬는 변경이었다. | `docs/README.md` / request lifecycle 문서 목록 | async UI의 품질 설명은 기능 목록이 아니라 상태 전이 설명으로 써야 한다. | 실제 verify를 재실행한다. |
| 4 | 2026-03-13 / Reverify | canonical verify를 다시 실행해 request lifecycle contract를 현재 시점에 고정한다 | `vanilla/tests/service.test.ts`, `vanilla/tests/explorer.test.ts`, `vanilla/tests/explorer.spec.ts` | unit test만으론 retry와 keyboard viability를 충분히 보여 주기 어렵다고 봤다. | `npm run verify --workspace @front-react/networked-ui-patterns`를 다시 실행해 unit + Playwright 결과를 확보했다. | `cd study && npm run verify --workspace @front-react/networked-ui-patterns` | unit `4 passed`, Playwright `2 passed (4.0s)` | `vanilla/tests/explorer.spec.ts` / retry + navigation 시나리오 | async UI는 state helper correctness와 화면 흐름 복구를 함께 봐야 한다. | SSR, auth, real cache가 없는 현재 경계를 적는다. |
