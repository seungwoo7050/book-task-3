# 03 Hooks And Events evidence ledger

이 단계는 state, effect, event를 따로 설명하면 오히려 흐려진다. `runtime.ts`가 한 파일 안에서 세 가지를 동시에 다루기 때문에 chronology도 그 합류 지점을 중심으로 재구성했다.

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-03-08 / Phase 1 | 이 단계의 범위가 "hook trivia"가 아니라 runtime integration임을 먼저 고정한다 | `README.md`, `problem/README.md`, `docs/README.md`, `ts/README.md` | hooks와 events를 따로 떨어진 주제로 읽게 될 위험이 커서, README에서부터 runtime integration 문제라는 점을 먼저 봐야 한다고 생각했다. | `useState`, `useEffect`, delegated event, runtime integration tests가 한 세트라는 public contract를 먼저 정리했다. | `git log --reverse --stat -- study/react-internals/03-hooks-and-events | sed -n '1,40p'` | 2026-03-08 commit에 `runtime.ts`와 state/effect/events/integration tests가 같이 들어왔다. | `README.md` / 핵심 구현 포인트 | 이 단계의 본론은 API 개수보다 runtime 안에서 state/effect/event가 어떻게 연결되는가다. | delegated event와 hook slot을 먼저 읽는다. |
| 2 | 2026-03-08 / Phase 2 | runtime metadata와 delegated event 흐름을 고정한다 | `ts/src/runtime.ts`, `ts/tests/events.test.ts`, `ts/tests/integration.test.ts` | hook 구현이 먼저일 것 같지만, event delegation과 metadata sync가 없으면 runtime tree 설명이 비어 있다고 봤다. | `dispatchDelegatedEvent`, `syncEventListeners`, `commitRoot`를 중심으로 runtime tree와 DOM metadata 연결을 읽었다. | `rg -n 'dispatchDelegatedEvent|syncEventListeners|scheduleRender' study/react-internals/03-hooks-and-events/ts/src/runtime.ts` | delegated event와 listener sync 지점이 runtime metadata 설계의 중심 anchor로 잡혔다. | `ts/src/runtime.ts` / `dispatchDelegatedEvent` | delegated event는 DOM bubbling 재사용이 아니라 runtime tree metadata를 유지하는 문제와 함께 움직인다. | hook slot과 effect timing으로 내려간다. |
| 3 | 2026-03-08 / Phase 3 | hook slot invariant와 effect timing을 같은 runtime loop에 넣는다 | `ts/src/runtime.ts`, `ts/tests/state.test.ts`, `ts/tests/effect.test.ts` | `useState`와 `useEffect`를 별도 helper로만 보면 hook order invariant가 흐려질 거라고 봤다. | `useState`, `useEffect`, `scheduleRender`, pending effect queue를 읽으며 slot index와 cleanup timing을 확인했다. | `rg -n 'useState|useEffect' study/react-internals/03-hooks-and-events/ts/src/runtime.ts` | `useState`, `useEffect`가 같은 `currentHookContext`와 slot index를 공유하고 있었다. | `ts/src/runtime.ts` / `useState`, `useEffect` | hook의 핵심은 문법이 아니라 render 순서와 slot index를 깨지 않는 invariant다. | verify와 typecheck로 runtime contract를 닫는다. |
| 4 | 2026-03-13 / Reverify | canonical verify로 state/effect/event integration을 다시 고정한다 | `ts/tests/state.test.ts`, `ts/tests/effect.test.ts`, `ts/tests/events.test.ts`, `ts/tests/integration.test.ts`, `tsconfig.json` | runtime이 복잡해 보이지만, 현재 단계는 correctness와 integration test만 명확하면 충분하다고 판단했다. | `npm run verify --workspace @front-react/hooks-and-events`를 다시 실행해 `7 passed`와 typecheck 통과를 확보했다. | `cd study && npm run verify --workspace @front-react/hooks-and-events` | `Test Files 4 passed`, `Tests 7 passed`, `tsc --noEmit` 통과 | `ts/tests/integration.test.ts` / runtime integration assertions | runtime 단계는 feature checklist보다 ordering과 cleanup correctness가 중요하다. | consumer app 단계로 넘긴다. |
