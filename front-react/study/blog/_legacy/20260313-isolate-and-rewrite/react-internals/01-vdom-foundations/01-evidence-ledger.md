# 01 VDOM Foundations evidence ledger

이 프로젝트는 `createElement`와 `render`까지의 최소 경로를 한 패키지 안에 압축해 둔 형태라, chronology도 `VNode shape -> DOM reflection -> verify` 순서로 복원하는 편이 가장 자연스럽다.

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-03-08 / Phase 1 | adapted 문제 범위와 package surface를 먼저 고정한다 | `README.md`, `problem/README.md`, `problem/original/README.md`, `ts/README.md`, `package.json` | internals 프로젝트라면 diff부터 다룰 것 같지만, README를 보면 이 단계는 의도적으로 `createElement -> render`까지만 다룬다. | adapted README와 original README를 함께 읽고, 다음 단계가 소비할 package boundary라는 점을 먼저 고정했다. | `git log --reverse --stat -- study/react-internals/01-vdom-foundations | sed -n '1,40p'` | 2026-03-08 commit에 problem/original, `ts/src/*`, tests, `vitest.config.ts`가 함께 들어왔다. | `README.md` / 범위 설명 | React mental model을 설명하려면 먼저 무엇을 의도적으로 제외했는지도 분명해야 한다. | VNode 생성 규칙부터 읽는다. |
| 2 | 2026-03-08 / Phase 2 | JSX-like 호출을 VNode shape로 고정한다 | `ts/src/element.ts`, `ts/tests/element.test.ts` | child 정규화 없이도 DOM render만 되면 될 것 같지만, primitive child를 `TEXT_ELEMENT`로 고정하지 않으면 뒤 단계가 흔들린다고 봤다. | `createTextElement`, `createElement`를 중심으로 `children` 배열과 text normalization 규칙을 확인했다. | `rg -n 'createTextElement|createElement' study/react-internals/01-vdom-foundations/ts/src/element.ts` | `createTextElement`와 `createElement` 두 함수가 이 단계의 데이터 모델을 거의 전부 설명했다. | `ts/src/element.ts` / `createTextElement`, `createElement` | Virtual DOM의 첫 invariants는 "모든 child가 같은 shape로 정규화된다"는 점이다. | DOM reflection과 prop/event 반영으로 내려간다. |
| 3 | 2026-03-08 / Phase 3 | VNode를 실제 DOM 조작으로 연결한다 | `ts/src/dom-utils.ts`, `ts/tests/dom-utils.test.ts`, `ts/src/index.ts` | VNode 생성과 DOM 반영을 같은 함수에 섞어도 될 것 같지만, update와 render를 설명하려면 helper를 분리해야 한다고 봤다. | `createDom`, `updateDom`, `render`를 읽으며 prop/event 반영과 재귀 render 흐름을 분리했다. | `rg -n 'createDom|updateDom|render' study/react-internals/01-vdom-foundations/ts/src/dom-utils.ts` | `createDom`, `updateDom`, `render`가 prop/event reflection과 child traversal을 각각 맡고 있었다. | `ts/src/dom-utils.ts` / `updateDom`, `render` | DOM mutation과 VNode shape를 분리해야 다음 단계의 diff/patch가 설명 가능해진다. | verify와 typecheck로 package contract를 닫는다. |
| 4 | 2026-03-13 / Reverify | canonical verify로 package contract를 현재 시점에 다시 고정한다 | `ts/tests/element.test.ts`, `ts/tests/dom-utils.test.ts`, `tsconfig.json` | unit tests만 있어도 되지만, 다음 단계가 소비하는 패키지이므로 typecheck까지 함께 봐야 한다고 판단했다. | `npm run verify --workspace @front-react/vdom-foundations`를 다시 실행해 vitest `27 passed`와 typecheck 통과를 확인했다. | `cd study && npm run verify --workspace @front-react/vdom-foundations` | `Test Files 2 passed`, `Tests 27 passed`, `tsc --noEmit` 통과 | `ts/tests/dom-utils.test.ts` / DOM reflection assertions | foundational package는 기능 데모보다 "다음 패키지가 import해도 shape가 흔들리지 않는다"는 점이 중요하다. | diff/patch가 없는 현재 한계를 마지막에 분리한다. |
