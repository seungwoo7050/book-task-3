# 04 Runtime Demo App evidence ledger

이 프로젝트는 internals 트랙의 마감 단계라서, runtime 자체보다 "그 runtime을 실제 앱이 어디까지 버티는가"를 보여 주는 근거가 중요하다. chronology도 consumer app의 시나리오와 verify를 중심으로 세웠다.

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-03-08 / Phase 1 | shared runtime consumer app이라는 범위를 먼저 고정한다 | `README.md`, `problem/README.md`, `docs/README.md`, `ts/README.md` | demo app이면 시각 요소가 중심일 것 같지만, README는 runtime code를 복사하지 않고 package를 import하는 점을 더 강조하고 있었다. | consumer app, debounced search, pagination, metrics라는 네 가지 public contract를 먼저 묶었다. | `git log --reverse --stat -- study/react-internals/04-runtime-demo-app | sed -n '1,40p'` | 2026-03-08 commit에 `ts/src/app.ts`, `ts/src/data.ts`, `ts/tests/demo.test.ts`가 함께 들어왔다. | `README.md` / 핵심 구현 포인트 | internals 마감 단계에서는 UI보다 shared runtime consumption 자체가 중요한 증거가 된다. | debounced search와 metrics update를 읽는다. |
| 2 | 2026-03-08 / Phase 2 | runtime consumer app의 상호작용 핵심을 만든다 | `ts/src/app.ts`, `ts/src/data.ts` | 검색과 페이지네이션만 있으면 될 줄 알았지만, README를 보면 metrics panel이 runtime 학습용 관찰값으로 함께 있어야 했다. | `useDebouncedValue`, `updateMetrics`, `DemoApp`을 중심으로 search, load-more, metrics가 한 render loop에 들어가는 지점을 읽었다. | `rg -n 'useDebouncedValue|updateMetrics|DemoApp|mountRuntimeDemo' study/react-internals/04-runtime-demo-app/ts/src/app.ts` | `useDebouncedValue`, `updateMetrics`, `DemoApp` 세 지점이 상호작용과 관찰값의 핵심 anchor였다. | `ts/src/app.ts` / `useDebouncedValue`, `updateMetrics` | consumer app은 기능 데모이면서 동시에 runtime의 한계를 드러내는 측정 창 역할도 한다. | verify로 debounce/pagination/metrics를 함께 확인한다. |
| 3 | 2026-03-13 / Reverify | canonical verify로 consumer app 시나리오를 다시 고정한다 | `ts/tests/demo.test.ts`, `tsconfig.json` | visual demo라서 수동 확인이 필요할 것 같지만, 현재 범위는 integration-style test와 typecheck만으로 충분히 닫힌다고 판단했다. | `npm run verify --workspace @front-react/runtime-demo-app`를 다시 실행해 `3 passed`와 `tsc --noEmit` 통과를 확인했다. | `cd study && npm run verify --workspace @front-react/runtime-demo-app` | `Test Files 1 passed`, `Tests 3 passed`, `tsc --noEmit` 통과 | `ts/tests/demo.test.ts` / debounce + load more + metrics assertions | consumer app 단계에서도 runtime 이해는 여전히 testable contract로 닫을 수 있다. | 제품형 UI 신호는 다음 트랙 `frontend-portfolio`로 넘긴다. |
