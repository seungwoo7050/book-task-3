# 02 Render Pipeline evidence ledger

이 단계는 diff, patch, scheduler를 따로 배워 놓은 조각 지식을 하나의 pipeline으로 묶는 순간이 핵심이다. 그래서 chronology도 세 파일의 역할 분리와 verify 기준을 중심으로 세웠다.

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-03-08 / Phase 1 | foundation 위에 무엇을 추가할지 범위를 먼저 고정한다 | `README.md`, `problem/README.md`, `problem/original/README.md`, `ts/README.md` | DOM을 조금 더 똑똑하게 다시 그리는 정도일 줄 알았지만, README는 명확히 render/commit 분리까지 요구하고 있었다. | diff/patch/scheduler 세 축과 `flushSync`까지 포함하는 public contract를 먼저 고정했다. | `git log --reverse --stat -- study/react-internals/02-render-pipeline | sed -n '1,40p'` | 2026-03-08 commit에 `diff.ts`, `patch.ts`, `scheduler.ts`, tests가 한 번에 들어왔다. | `README.md` / 핵심 구현 포인트 | render pipeline은 최적화 트릭이 아니라 phase separation 문제다. | diff scope부터 읽는다. |
| 2 | 2026-03-08 / Phase 2 | 무엇이 바뀌었는지 계산하는 규칙을 먼저 세운다 | `ts/src/diff.ts`, `ts/tests/diff.test.ts` | keyed/unkeyed child diff를 같은 방식으로 처리해도 될 것 같지만, reorder 설명이 흐려질 거라고 봤다. | `diffChildrenByKey`, `diffChildren`, `diff`를 따라 keyed/unkeyed 분기와 patch shape를 고정했다. | `rg -n 'diffChildrenByKey|diffChildren|diff\\(' study/react-internals/02-render-pipeline/ts/src/diff.ts` | `diffChildrenByKey`, `diffChildren`, `diff`가 patch calculation의 중심 anchor로 잡혔다. | `ts/src/diff.ts` / `diffChildrenByKey` | reconciliation 설명의 핵심은 "어떤 patch가 필요한가"를 먼저 계산하는 데 있다. | patch ordering과 commit 시점을 읽는다. |
| 3 | 2026-03-08 / Phase 3 | render phase와 commit phase를 실제 코드로 분리한다 | `ts/src/patch.ts`, `ts/src/scheduler.ts`, `ts/tests/patch.test.ts`, `ts/tests/scheduler.test.ts` | patch 함수만 있으면 충분할 것 같지만, commit 시점이 분리되지 않으면 interrupted work를 설명할 수 없다고 봤다. | `commitRoot`, `workLoop`, `flushSync`를 중심으로 render/commit split과 cooperative work loop를 읽었다. | `rg -n 'commitRoot|workLoop|flushSync' study/react-internals/02-render-pipeline/ts/src/scheduler.ts` | `commitRoot`, `workLoop`, `flushSync` 세 지점이 pipeline phase split의 핵심이었다. | `ts/src/scheduler.ts` / `workLoop`, `commitRoot` | render 단계에서 DOM mutation을 미루는 이유가 scheduler 한계와 직접 연결된다는 점을 배웠다. | verify와 typecheck로 pipeline contract를 닫는다. |
| 4 | 2026-03-13 / Reverify | canonical verify로 diff/patch/scheduler contract를 다시 고정한다 | `ts/tests/diff.test.ts`, `ts/tests/patch.test.ts`, `ts/tests/scheduler.test.ts`, `tsconfig.json` | unit tests만 빠르게 돌리면 될 것 같지만, 패키지 경계가 있으므로 typecheck까지 같이 봐야 한다고 판단했다. | `npm run verify --workspace @front-react/render-pipeline`를 다시 실행해 `8 passed`와 `tsc --noEmit` 통과를 확보했다. | `cd study && npm run verify --workspace @front-react/render-pipeline` | `Test Files 3 passed`, `Tests 8 passed`, `tsc --noEmit` 통과 | `ts/tests/scheduler.test.ts` / interrupted work assertions | pipeline 프로젝트는 성능 주장보다 phase split correctness를 먼저 검증해야 한다. | hooks/events가 없는 현재 경계를 적고 다음 단계로 넘긴다. |
