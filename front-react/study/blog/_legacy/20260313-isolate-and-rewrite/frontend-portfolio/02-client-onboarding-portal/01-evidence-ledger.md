# 02 Client Onboarding Portal evidence ledger

이 프로젝트는 고객-facing flow답게 route, validation, draft, submit retry가 한 연속 경험으로 엮여야 한다. chronology도 그 사용자 흐름에 맞춰 route gate와 portal mutations를 중심으로 재구성했다.

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-03-08 / Phase 1 | 고객-facing onboarding flow의 product surface를 먼저 고정한다 | `README.md`, `problem/README.md`, `next/README.md`, `package.json` | form app라서 validation schema부터 볼 수 있지만, README를 보면 session gate와 route flow가 먼저였다. | sign-in, onboarding wizard, draft restore, submit retry를 public contract로 먼저 묶었다. | `git log --reverse --stat -- study/frontend-portfolio/02-client-onboarding-portal | sed -n '1,40p'` | 2026-03-08 commit에 route/app, portal component, lib helpers, tests가 함께 들어왔다. | `README.md` / 핵심 구현 포인트 | 고객-facing flow에서는 screen 수보다 route continuity가 먼저다. | route gate와 session query를 읽는다. |
| 2 | 2026-03-08 / Phase 2 | route와 session gate를 명시적으로 분리한다 | `next/src/components/portal/onboarding-route.tsx`, `next/src/components/portal/sign-in-panel.tsx`, `next/src/lib/guards.ts`, `next/tests/unit/guards.test.ts` | onboarding wizard가 메인이라 route wrapper는 얇을 줄 알았지만, direct access guard와 sign-in 분기가 흐름 전체를 좌우한다고 봤다. | `OnboardingRoute`, `SignInPanel`, `coerceStep`을 중심으로 URL step과 session state를 분리했다. | `rg -n 'OnboardingRoute|ClientOnboardingPortal|onStepChange' study/frontend-portfolio/02-client-onboarding-portal/next/src/components/portal/onboarding-route.tsx study/frontend-portfolio/02-client-onboarding-portal/next/src/components/portal/client-onboarding-portal.tsx` | `OnboardingRoute`와 `ClientOnboardingPortal` 연결이 route guard surface의 핵심으로 보였다. | `next/src/components/portal/onboarding-route.tsx` / `router.replace` | 고객-facing flow에서는 route query와 form state를 섞지 않는 것이 UX와 복원성을 모두 지킨다. | portal 내부 draft/save/submit mutations로 내려간다. |
| 3 | 2026-03-08 / Phase 3 | draft restore와 submit retry를 같은 portal loop에 묶는다 | `next/src/components/portal/client-onboarding-portal.tsx`, `next/src/lib/storage.ts`, `next/src/lib/schemas.ts`, `next/tests/integration/client-onboarding-portal.test.tsx` | 저장/제출을 각각의 form action으로만 보면 될 것 같았지만, onboarding은 profile, invite, checklist, submit 상태가 계속 이어져야 했다. | `ClientOnboardingPortal`에서 query, `useForm`, `saveProfileMutation`, `submitMutation`, `canSubmit`를 함께 읽어 multi-step flow를 하나의 portal loop로 묶었다. | `rg -n 'ClientOnboardingPortal|saveProfileMutation|submitMutation' study/frontend-portfolio/02-client-onboarding-portal/next/src/components/portal/client-onboarding-portal.tsx` | `saveProfileMutation`과 `submitMutation`이 draft/save/retry를 실제 흐름으로 닫는 핵심 anchor였다. | `next/src/components/portal/client-onboarding-portal.tsx` / `submitMutation`, `canSubmit` | 고객-facing onboarding에서는 validation correctness뿐 아니라 "실패 뒤 다시 이어가기"가 제품 경험의 핵심이다. | verify로 route guard와 retry 시나리오를 닫는다. |
| 4 | 2026-03-13 / Reverify | canonical verify로 onboarding flow contract를 다시 고정한다 | `next/tests/unit/*`, `next/tests/integration/*`, `next/tests/e2e/*`, `next/tsconfig.json` | 통합 test만 봐도 될 것 같지만, 고객-facing flow는 route guard와 E2E success/failure path까지 함께 봐야 한다고 판단했다. | `npm run verify --workspace @front-react/client-onboarding-portal`를 다시 실행해 typecheck, `7`개 tests, `2`개 E2E 흐름을 확인했다. | `cd study && npm run verify --workspace @front-react/client-onboarding-portal` | typecheck 통과, vitest `7 passed`, Playwright `2 passed (12.9s)` | `next/tests/e2e/client-onboarding.spec.ts` / direct access guard + draft restore/retry | multi-step flow는 screen completeness보다 route와 recovery path가 현재 시점에도 통과하는지가 중요하다. | 실제 auth/email delivery가 없는 현재 경계를 마지막에 적는다. |
