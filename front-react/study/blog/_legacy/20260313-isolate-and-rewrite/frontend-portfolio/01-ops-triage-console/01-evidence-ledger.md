# 01 Ops Triage Console evidence ledger

이 프로젝트는 코드 양이 많지만, 실제 판단 전환점은 몇 군데로 압축된다. product surface를 먼저 고정하고, optimistic mutation과 dense queue workflow를 핵심 anchor로 잡는 편이 chronology를 가장 잘 보존한다.

| 순서 | 시간 표지 | 당시 목표 | 변경 단위 | 처음 가설 | 실제 조치 | CLI | 검증 신호 | 핵심 코드 앵커 | 새로 배운 것 | 다음 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-03-08 / Phase 1 | 운영 콘솔의 product surface를 먼저 고정한다 | `README.md`, `problem/README.md`, `next/README.md`, `package.json` | large UI 프로젝트라서 컴포넌트 tree부터 볼 유혹이 있었지만, README를 보니 dashboard, queue, bulk action, retry/undo가 먼저 계약으로 잡혀 있었다. | internal tool triage workflow와 `typecheck + vitest + playwright` verify surface를 먼저 고정했다. | `git log --reverse --stat -- study/frontend-portfolio/01-ops-triage-console | sed -n '1,40p'` | 2026-03-08 commit에 Next app, hooks, lib helpers, tests, presentation assets가 함께 들어왔다. | `README.md` / 검증 섹션 | 포트폴리오 앱은 UI 트리보다 workflow contract를 먼저 읽는 편이 낫다. | optimistic update와 rollback 경로를 읽는다. |
| 2 | 2026-03-08 / Phase 2 | query cache를 optimistic mutation과 rollback/undo에 맞춘다 | `next/src/hooks/use-ops-triage.ts`, `next/tests/unit/optimistic.test.ts`, `next/tests/integration/ops-triage-console.test.tsx` | detail update와 list update를 따로 갱신해도 될 것 같지만, 운영 콘솔은 list/detail/saved view가 동시에 일관돼야 한다고 봤다. | `useIssueMutation`과 `useBulkIssueMutation`에서 query snapshot, optimistic patch, rollback, undo 흐름을 한 mutation pipeline으로 읽었다. | `rg -n 'useIssueMutation|useBulkIssueMutation|useIssueList' study/frontend-portfolio/01-ops-triage-console/next/src/hooks/use-ops-triage.ts` | `useIssueMutation`, `useBulkIssueMutation`, `useIssueList`가 triage state orchestration의 핵심 anchor였다. | `next/src/hooks/use-ops-triage.ts` / `useIssueMutation` | product형 프론트에서 optimistic UI의 본질은 "빨라 보이기"가 아니라 rollback 경로까지 포함한 consistency다. | dense queue UI가 이 mutation contract를 어떻게 소비하는지 본다. |
| 3 | 2026-03-08 / Phase 3 | dense queue, saved view, keyboard flow를 하나의 console surface로 묶는다 | `next/src/components/console/ops-triage-console.tsx`, `next/tests/e2e/ops-triage.spec.ts` | 화면이 크니 컴포넌트를 잘게 나누는 일이 핵심처럼 보였지만, 실제 전환점은 deferred search와 table selection을 하나의 operator workflow로 묶는 데 있었다. | `OpsTriageConsole`에서 `useDeferredValue`, query hooks, table selection, bulk draft, toast 흐름이 어떻게 이어지는지 좁혔다. | `rg -n 'OpsTriageConsole|useDeferredValue|useIssueList|useIssueMutation' study/frontend-portfolio/01-ops-triage-console/next/src/components/console/ops-triage-console.tsx study/frontend-portfolio/01-ops-triage-console/next/src/hooks/use-ops-triage.ts` | `OpsTriageConsole`, `useDeferredValue`, `useIssueList`, `useIssueMutation`이 workflow surface의 중심이었다. | `next/src/components/console/ops-triage-console.tsx` / `deferredSearch`, table selection | data-heavy UI는 화면 수보다 operator가 끊기지 않고 triage를 끝낼 수 있는 흐름이 더 중요하다. | verify로 update, bulk, retry, keyboard path를 닫는다. |
| 4 | 2026-03-13 / Reverify | canonical verify로 product contract를 현재 시점에 다시 고정한다 | `next/tests/unit/*`, `next/tests/integration/*`, `next/tests/e2e/*`, `next/tsconfig.json` | 테스트 종류가 많아서 일부만 봐도 될 것 같지만, 이 프로젝트는 typecheck/unit/integration/E2E가 모두 있어야 portfolio signal이 닫힌다고 판단했다. | `npm run verify --workspace @front-react/ops-triage-console`를 다시 실행해 typecheck, `16`개 tests, `4`개 E2E 흐름을 확인했다. | `cd study && npm run verify --workspace @front-react/ops-triage-console` | typecheck 통과, vitest `16 passed`, Playwright `4 passed (28.0s)` | `next/tests/e2e/ops-triage.spec.ts` / undo, bulk update, retry, keyboard triage | portfolio 앱은 "동작했다"보다 어떤 operator workflow를 끝까지 통과시켰는지가 더 중요하다. | 실제 auth/DB가 없는 현재 경계를 마지막에 적는다. |
