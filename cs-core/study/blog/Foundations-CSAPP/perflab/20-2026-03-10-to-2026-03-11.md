# Performance Lab 개발 타임라인 (2026-03-10 ~ 2026-03-11)

## 2026-03-10
### Session 1
`64x64`는 이 프로젝트에서 가장 어려운 구간이었다. `32x32`에서 먹혔던 8x8 block + diagonal 보관을 그대로 적용하면 miss가 `1800`대. 목표는 `<1300`.

처음엔 "block size를 줄이면 되지 않나" 하고 4x4로 해봤다. 더 나빠졌다. 왜? block이 작아지면 loop overhead가 늘어나고, 더 많은 column 접근이 cache line을 밀어낸다.

여기서 방향을 바꿨다. 8x8 block을 유지하되, 상단 4행과 하단 4행을 분리해서 처리한다. 상단 4행을 먼저 읽어서 B의 왼쪽 절반에 쓰고, 하단 4행에서 온 데이터를 B의 오른쪽 절반에 쓴 뒤, B에 잘못 놓인 상단 4행의 오른쪽 절반을 정위치로 swap한다.

이 아이디어가 떠오르기까지 cache miss 패턴을 세 번 정도 다시 그렸다. 핵심 통찰은 `64x64`에서 row가 4개씩 cache set을 공유하기 때문에, 8행을 한 번에 처리하면 상단이 하단에 의해 evict된다는 것이었다. "절반씩 처리하면 eviction을 피할 수 있다"는 결론이 여기서 나왔다.

```c
    for (ii = 0; ii < 64; ii += 8) {
        for (jj = 0; jj < 64; jj += 8) {
            int i;
            for (i = ii; i < ii + 4; ++i) {
                int a0 = load_a(64, A, i, jj+0, cache);
                int a1 = load_a(64, A, i, jj+1, cache);
                int a2 = load_a(64, A, i, jj+2, cache);
                int a3 = load_a(64, A, i, jj+3, cache);
                int a4 = load_a(64, A, i, jj+4, cache);
                int a5 = load_a(64, A, i, jj+5, cache);
                int a6 = load_a(64, A, i, jj+6, cache);
                int a7 = load_a(64, A, i, jj+7, cache);
                store_b(64, B, jj+0, i, a0, cache);
                store_b(64, B, jj+1, i, a1, cache);
                store_b(64, B, jj+2, i, a2, cache);
                store_b(64, B, jj+3, i, a3, cache);
                store_b(64, B, jj+0, i+4, a4, cache);
                store_b(64, B, jj+1, i+4, a5, cache);
                store_b(64, B, jj+2, i+4, a6, cache);
                store_b(64, B, jj+3, i+4, a7, cache);
            }
            for (i = 0; i < 4; ++i) {
                int a0 = load_a(64, A, ii+4, jj+i, cache);
                int a1 = load_a(64, A, ii+5, jj+i, cache);
                int a2 = load_a(64, A, ii+6, jj+i, cache);
                int a3 = load_a(64, A, ii+7, jj+i, cache);
                int b0 = load_b(64, B, jj+i, ii+4, cache);
                int b1 = load_b(64, B, jj+i, ii+5, cache);
                int b2 = load_b(64, B, jj+i, ii+6, cache);
                int b3 = load_b(64, B, jj+i, ii+7, cache);
                store_b(64, B, jj+i, ii+4, a0, cache);
                store_b(64, B, jj+i, ii+5, a1, cache);
                store_b(64, B, jj+i, ii+6, a2, cache);
                store_b(64, B, jj+i, ii+7, a3, cache);
                store_b(64, B, jj+i+4, ii+0, b0, cache);
                store_b(64, B, jj+i+4, ii+1, b1, cache);
                store_b(64, B, jj+i+4, ii+2, b2, cache);
                store_b(64, B, jj+i+4, ii+3, b3, cache);
            }
        }
    }
```

이 코드가 길어 보이지만, 하는 일은 명확하다. (1) A의 상단 4행 전체를 읽어서 B의 왼쪽 절반에 쓰고 오른쪽 절반은 B의 "임시 자리"에 넣는다. (2) A의 하단 4행을 읽으면서 B의 임시 자리에 있던 데이터를 정위치로 swap한다.

`64x64` miss가 `1176`으로 목표 `<1300` 안에 들어왔다.

CLI:

```bash
$ cd c
$ make clean && make test
$ cd ../cpp
$ make clean && make test
```

C++ 구현도 같은 miss 결과를 유지.

## 2026-03-11
### Session 1
최종 정리. 세 크기별 miss 기준: `32x32` = 284, `64x64` = 1176, `61x67` = 1989.

이 프로젝트에서 가장 중요한 교훈은 "빠르다"가 아니라 "왜 이 접근 순서에서 이 miss 수가 나오는가"를 설명할 수 있느냐 였다. 숫자만 남기면 아무 의미 없고, 그 숫자가 cache 구조와 어떻게 연결되는지 설명 가능해야 학습 기록이 된다.

CLI:

```bash
$ cd problem && make status && make compile
$ cd ../c && make test
$ cd ../cpp && make test
```

starter boundary와 구현 boundary가 같은 문서 표면 안에서 구분됨.
