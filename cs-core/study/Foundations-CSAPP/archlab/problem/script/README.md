# Official Simulator Workflow

The original Architecture Lab helper scripts live inside the restored official handout under
`../official/archlab-handout/sim/` after `make restore-official`.

The tracked study-owned official hand-in files live in `../y86/`, and `make verify-official`
copies them into that restored simulator tree before running the official Part A/B/C checks in
Docker.
