# Problem Boundary Notes

The tracked study-owned official hand-in files now live under `../y86/`.

When you run `make restore-official` and `make verify-official` from `problem/`, the public
self-study handout is restored under the ignored local directory `../official/`, and the files from
`../y86/` are copied into that restored simulator tree for official verification.
