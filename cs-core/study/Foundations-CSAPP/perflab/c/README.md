# C Track

## Scope

This directory contains the fresh C implementation for Performance Lab.

It includes:

- a trace-driven cache simulator with LRU replacement
- an instrumented transpose benchmark that models the official direct-mapped cache
- correctness checks plus benchmark thresholds for the official matrix sizes

## Commands

```bash
cd c
make clean && make test
```
