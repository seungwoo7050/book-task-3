.PHONY: all status clean

all: status

status:
	@echo "shlab problem/ is now a contract-only boundary for strict public release."
	@echo "No copied starter shell, official traces, or bundled driver are redistributed here."
	@echo "Active verification lives in c/tests and cpp/tests."

clean:
	@true
