# Removed Starter Driver

The copied Perl trace driver was removed from the strict public-release version of `study/`.

Signal and job-control verification now uses study-owned direct harnesses under:

- `../../tests/`
- `../../c/tests/`
- `../../cpp/tests/`
