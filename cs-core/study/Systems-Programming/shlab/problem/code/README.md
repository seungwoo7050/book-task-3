# Removed Starter Assets

This directory used to hold a copied starter shell skeleton and helper header.

For strict public release, those copied starter assets were removed.

The active self-owned implementations live in:

- `../../c/src/tsh.c`
- `../../cpp/src/tsh.cpp`
